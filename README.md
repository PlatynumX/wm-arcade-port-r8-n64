# WWF WrestleMania Arcade -> N64 source port (r9 source-engine pass)

**Primary target:** Nintendo 64 / libdragon.  
**Porting rule:** when the arcade source implements it, translate that implementation and use its original data/assets. Do not replace it with invented N64 behavior.

r9 is the first broad **shared-engine** pass. It is not a claim that the complete arcade game is already playable. The point of this revision is to replace one-off bring-up code with reusable source-driven systems that whole parts of the original program can execute on.

## What r9 actually translates

- **53 Hz arcade source clock.** `DISPLAY.EQU`'s source tick rate runs independently of 60 Hz N64 presentation, so translated sleeps, timers, processes and animation cadence are no longer accelerated to 60 Hz.
- **Cooperative source-process scheduler.** Shared CREATE/sleep/kill-by-PID behavior is used by translated frontend processes instead of hand-coded screen timers.
- **Packed BMOD records.** The original 64-bit background block records are decoded generically; CI extracts `NTITLESCBMOD` and `SPORTBKBMOD` directly from `BGNDTBL.ASM`.
- **Original BLIMP title artwork.** `BIGWWF.BDB`/`BIGWWF.BDD` are decoded into their 40 CI8 blocks and six RGB555 palettes, cross-validated against `NTITLESCBMOD`, and rendered from the original packed block records rather than a flattened WIMP crop.
- **Original title sparkle artwork/process lifetime.** All 69 WIMP frames named by `MISC.LOD` from `SPARKLE.IMG` are generated as source sprites. `show_title` creates/kills the source `FLASH_PID` and `ATTRACT_ANIMPID` processes alongside `cycle_lava`. The shared `SPRINKLE_GLINTS`/`RANDOM_SPARKLE` helper bodies and `WHERE_WRESTLMANIA_SPARKLES` table are absent from the checked-in WWF tree, so placement/random cadence is clearly marked provisional rather than source-exact.
- **Typed animation stream.** Mixed WORD/LONG operands are preserved. Implemented commands execute source operand widths and direction rules; unknown commands fail closed instead of being guessed.
- **Whole-source dependency IR.** CI scans every `SUBR`/`SUBRP`, static call/process edge and unresolved dynamic edge, with frontiers rooted at `attract_mode` and `start_match`.
- **Whole-source animation IR.** CI mechanically preserves typed WORD/LONG animation data and W/L packing macros across the historical ASM tree.
- **Source selection core from `SELECT.ASM`.** The 2x4 cursor grid, exact crouton coordinates, source BMOD placement, player start/mug/sound data, `scramble_table` roster mapping, attributes, legal four-way movement, START+UP random-selection gate, random wander/home rules, 15-second source selection timer and 30-tick final wait constants are translated in portable C.
- **Source-driven attract execution.** Initial 8-tick blank, active source routine order, DCS/Midway/title timing, Midway background movement, title lava scheduling, and the source title sparkle process lifetime remain source-driven.
- **Original artwork paths already proven on N64.** Midway Sports uses the original 17 WIMP pieces; title art is derived from original artist/BMOD data; Bret remains the first wrestler with the current visual backend.
- **`show_gameplay`'s attract-mode `start_match` creation path.** `ATTRACT.ASM::show_gameplay` is just `WRESTLE.ASM::start_match` run with `PSTATUS==0`; the `#0plyr` branch's `RNDRNG0(7)`-skip-7 wrestler draw, `LIFEBAR.ASM::init_life_data` (`LIFE_MAX`=163), and PLYRNUM/PSIDE wrestler-actor creation are translated (`wm/match.h`), on the real `SLEEP 3*60` + `wait_on_butn 10*TSEC` timing. See the boundary below for what this does not yet do.
- **Bret's live control layer and a real visual backend for it.** Whichever actor draws `WM_ROSTER_BRET` runs the actual `wm_arcade_move_bret` dispatcher every tick, not a harness. `wm/bret_backend.h` resolves `WM_BRET_ANIM_STAND2/4`/`TORSO2/4` and 7 of Bret's light/power punch/kick animation ids to their real `wm_visual_sequence` data and drives two `wm_visual_state` tracks (body + torso) from it -- the merge adapter `wm/arcade/wm_arcade_bret.h` itself calls for. Every other animation id, and every other wrestler, still resolves to nothing.
- **Real movement physics, and a real walk cycle to go with it.** `wm/movement.h` translates `WRESTLE.ASM::convert_facing` and `set_velocities` exactly (per-direction velocity table, `walk_fast`/opponent-ONGROUND-or-DEAD 1.5x boost, backward-facing 0.9x reduction) and wires `execute_walk`'s `MOVE_DIR`/`OBJ_CONTROL` (`M_FLIPH`) dispatch as Bret's real `execute_walk` callback. `wm/bret_backend.h` also now runs `change_walk_anim`'s leg-cycle half: all 12 `hrt_walkM_fF_anim` sequences `hrt_leg_anims_table` (`BRET.ASM:2897`) can select are extracted and wired, so a Bret actor with real input genuinely walks with the correct animation, not just a position change under a frozen sprite. `change_walk_anim`'s torso half (`hrt_torso_anims_table`, `BRET.ASM:2981`) is fully wired too, diagonal and all 12 off-diagonal turn-transition entries -- see the next bullet.
- **`FACING_DIR`/`NEW_FACING_DIR` are real state now, and turning genuinely animates.** `wm/arcade/wm_arcade_closest.h`'s `wm_arcade_update_newfacing` translates `WRESTLE.ASM:3018`'s `update_newfacing` (live `NEW_FACING_DIR` toward the opponent, every tick, for every actor -- wired into `wm_match_tick` unconditionally, exactly where `WRESTLE.ASM`'s own per-process main loop calls it), and `wm/movement.h`'s `wm_execute_walk` translates `set_rotate_anim`'s literal `FACING_DIR=NEW_FACING_DIR` catch-up for the `#zip`/stance case (`WRESTLE.ASM:5082-5083`). Just like the source, `FACING_DIR` stays frozen at its last idle value while actually walking -- `change_walk_anim`'s leg half never writes it -- so facing one way while walking another (e.g. after running past a repositioned opponent) is representable. Both actors also start the match with real position *and* facing from the same real `WRESTLE.ASM:2691-2755` `#set0` placement init (`wm/match.h`'s `place_wrestler`, `#teamX_starts` table: `RING_X_CENTER-85,1127+93,WM_MOVE_UP_RIGHT` for P1, `RING_X_CENTER+85,1103+93,WM_MOVE_DOWN_LEFT` for the opponent) instead of a zeroed default -- the two positions are chosen so each wrestler's real starting facing already points at the other's real starting position, so nothing spuriously turns on tick 1. `set_rotate_anim`'s own turn-animation *selection* for the `#zip`/idle-turn case is now wired too (`wm_bret_rotate_anim`, `hrt_rotate_anims_table`), and `change_walk_anim`'s torso half genuinely reaches and animates the off-diagonal (mid-turn) region of `hrt_torso_anims_table` while walking, using the real `hrt_N_to_M_turn2_anim` sequences -- each carrying one or two real `ANI_SETFACING` commands (`HRTSEQ1.ASM:460-524`) that `wm_bret_backend_tick` fires at their exact hand-traced frame, which is what actually promotes `FACING_DIR` while genuinely walking. `execute_walk`'s own INTURN freeze (`WRESTLE.ASM:5222-5252`) is wired too and load-bearing: without it the instant `FACING_DIR` catch-up would truncate every turn animation almost immediately. The leg turn anims' own `ANI_XFLIP` (4 of the 6 canonical bodies -- the two adjacent-quadrant ones never cross the sprite's own left/right mirror line) is wired too, toggling `OBJ_CONTROL`'s `M_FLIPH` at its real, hand-traced frame index, same mechanism as the torso's `ANI_SETFACING`. Diagonal entries still reduce to picking between `hrt_torso2_anim` and `hrt_torso4_anim` (`hrt_torso8_anim`/`hrt_torso6_anim` are literal `SUBR` aliases of those two in `HRTSEQ1.ASM`, not separate artwork, confirmed by extracting and diffing their frame data). Translating this also surfaced a real pre-existing gap: Bret's 6 attack animations lead with a real `MODE_UNINT` (`HRTSEQ2.ASM`) this port's simplified `change_anim` never propagated onto the actor, so an attack could get silently stomped by the new idle-turn reselection on the same tick -- `wm_bret_backend_change_anim`/`wm_bret_backend_tick` now set and clear it for real, matching the source.
- **A real, human-playable single-player match for Bret.** `WM_APP_MODE_MATCH_INIT` (after select/pregame) is no longer a dead end: `wm_match_start_selected` translates `start_match`'s PSTATUS!=0 `#1plyr` path using whatever the select screen actually chose, and `wm/human_input.h` maps real controller state onto the same `but_val_cur`/`stick_val_cur` fields `DRONE.ASM`'s commit path writes for CPU wrestlers, so a human playing Bret drives the real `wm_arcade_move_bret` dispatch, real movement and walk animation, and the real visual backend above with actual button/stick presses.
- **Real attack-box activation for Bret's 6 wired attacks** (now 7 ids, see below). Each one now fires `ANIM.ASM`'s real `ATTACK_ON`/`ATTACK_ON_Z`/`ATTACK_OFF` (`wm_arcade_ani_attack_on(_z/_off)`, real and ctest-verified since fix38) at the exact frame hand-traced from its own `HRTSEQ2.ASM` source -- not guessed from the flattened `wm_visual_sequence` frame data, which (like every `wlanim.py --slice` extraction) linearly concatenates a source routine's branchy paths regardless of which one a real hit/block/miss actually takes. This sets a real `attack_mode`/`attack_xoff`/.../`WM_MODE_CHECKHIT` on the actor. 5 of the 6 attacks also carry a real, instant `ANI_SETFACING` right at their own start (`HRTSEQ2.ASM`, before any `WL` frame) -- `wm_bret_backend_change_anim` now translates it too, gated on the animation actually (re)starting so it fires once per selection like the source, not continuously; `hrt_4_kick_anim` is confirmed (by reading its own header) to be the one real exception with no `ANI_SETFACING` at all. Wiring this also turned up a small separate gap: `hrt_4_super_kick_anim` (`HRTSEQ2.ASM:1334-1335`) is a literal `SUBR` alias of `hrt_2_super_kick_anim`, not distinct artwork, so `WM_BRET_ANIM_SUPER_KICK4` now maps to the same real data and hand-traced attack window `SUPER_KICK2` already had, instead of staying unmapped.
- **Five more of Bret's real strikes are wired end to end, and the attack-window tracing is no longer by hand.** `tools/wlattack.py` reports, for each inline `ANIM.ASM` command in a routine, the 0-based index of the `WL` frame it immediately precedes -- reusing `tools/wlanim.py`'s own routine-scan and stop rules by importing it, so the two always agree on what "frame N" means. It reproduces all six previously hand-traced windows exactly (index and operands), which is what makes it trustworthy for animations nobody has traced yet. Using it, `WM_BRET_ANIM_BUTT2/BUTT4` (the close-range headbutt `do_punch` falls back to inside `near(50,45)`), `KNEE2/KNEE4` (the close-range knee `do_kick` falls back to), and `UPPERCUT4` (the crouching uppercut `do_super_punch` selects on a down input) now resolve to their real extracted `HRTSEQ2.ASM` frame data, carry their real `ANI_ATTACK_ON` windows (`AMODE_HDBUTT,19,75,35,24` at frame 5; `AMODE_KNEE,11,44,51,49` at frame 4; `AMODE_UPRCUT,-6,22,64,100` at frame 5), and get the real `ANI_SETMODE,MODE_UNINT|MODE_NOAUTOFLIP` + `ANI_SETFACING` header treatment the six already-wired attacks had. `src/generated/bret_frame_geometry.c` grew from 187 to 215 real frame geometries to match, so these five also produce a real per-frame `hurt_box` and can be confined and hit like the rest. Wiring them also exposed a genuine modelling error in `test_bret_backend_charge_face_rake_fires_on_release`: it held PUNCH for 100 ticks without ever advancing the animation, so the headbutt that press really selects stayed mid-swing forever. It only passed before because `BUTT2/4` resolved to nothing and therefore never set `MODE_UNINT`; the test now ticks the backend like the live match loop does, and `wm_arcade_bret_release_charge_face_rake`'s real `MODE_UNINT` precondition is exercised instead of bypassed.
- **Bret's stomp and ground punch, and attack windows that fire more than once.** `wm_bret_attack_window_t` used to hold exactly one `ANI_ATTACK_ON` per animation id, which is all eleven previously-wired attacks happen to have. `hrt_2/4_stomp_anim` genuinely fire two pulses each (an `AMODE_HITCHECK` probe, then the `AMODE_STOMP2` hit) and `hrt_2/4_ground_punch_anim` three (a probe, then two separate `AMODE_LBOWDROP2` elbow drops), each a distinct attack box with a real `ANI_ATTACK_OFF` between them. The table is now keyed on (id, frame) instead of id alone -- one row per real pulse -- so the whole `ON`/`OFF`/`ON` shape the source writes out is reproduced instead of silently dropping every pulse after the first. With that, `WM_BRET_ANIM_STOMP2/STOMP4` (what `do_kick` selects on a grounded opponent inside `near(160,140)`) and `GROUND_PUNCH2/GROUND_PUNCH4` (what `do_punch` selects on the same test) resolve to real frame data and real attack boxes; `src/generated/bret_frame_geometry.c` grew 215 -> 247 real frame geometries to match. These four are also the only wired attacks whose own `ANI_SETMODE` header adds `MODE_OVERLAP` (real, and genuinely read by `wm_arcade_combat.c`'s wrestler-overlap test), and the two stomps alone lead with an explicit `ANI_SETPLYRMODE,MODE_NORMAL` -- both now translated, and neither of them applied to the attacks that don't carry them. Reading every wired attack's header through `tools/wlattack.py` at once also showed all of them lead with an `ANI_ZEROVELS` before frame 0 that this port had never fired, so that is now wired uniformly for all of them rather than only for the new ones. `scripts/regenerate_source_data.sh` was brought back in sync at the same time: it had drifted to six sequences while `src/generated/bret_attacks.c` carried eleven and `bret_defense.c` wasn't regenerated by it at all, so the generated files could no longer be reproduced from the script that claims to produce them; re-running the updated script now reproduces `bret_defense.c` byte-identical and `bret_attacks.c` purely additively.
- **The tool now decides which animations can honestly be wired, and five more are.** Picking candidates by eye turned out to be unreliable: four animations that looked like obvious plain strikes (`BUTTS2/4`, `HAIR_PICKUP2/4`, `SHOOTER2/4`, `RAISE_ARM2/4`) are, on reading their real source, nothing of the kind -- `hrt_2/4_butts_anim` is a button-mash **repeat loop** that fires its attack once per iteration and ends by turning into the uppercut (`ANI_CHANGEANIM`); `hrt_2_hair_pickup_anim` and `hrt_2_shooter_anim` have no `ANI_END` at all and **fall through** into their own 4-facing twin; `hrt_2_raise_arm_anim` `ANI_GOTO`s into the middle of `hrt_4_raise_arm_anim`; and the 4-facing hair pickup and shooter are full puppet/grapple routines (`LEAPATOPP`, `ANI_ATTACHZ`, `ANI_SETOPPMODE`, `ANI_SUPERSLAVE2` against a puppet table). A flat `wlanim.py --slice` of any of those is not a truncation of one real playthrough, it is a frame list no playthrough ever plays. So `tools/wlattack.py --audit` now answers that question directly, sorting each routine's control flow into **blocking** (backward loop, `ANI_CHANGEANIM`, fall-through, a jump out of the routine, `ANI_SUPERSLAVE` rows whose frames `wlanim.py` cannot even parse), **unported** (`LEAPATOPP`, `ANI_ADD_MOVE`, `ANI_ATTACH` -- the frame list is fine, the gameplay command is not translated) and **tolerated** (forward skips and hit/miss forks, which is exactly what every already-wired attack has). Run against the 15 attacks already shipped, it clears all 15 -- that check is now a test, so the audit cannot quietly start calling shipped work broken. Run against everything else Bret's dispatcher can select, it named six honest candidates, of which five are now wired: `PUSH4` (the shove `mode_block` uses to break a blocker off), `JUMP_KICK4`, `KNEE_FALL4`, `KICK_TB` and `HEAD_HELD_STAND3`. `JUMP_KICK4` in particular had been living on a documented one-tick `MODE_UNINT` stopgap because it had no frame data to time a real one from; it now gets properly-timed protection like every other wired attack, and that stopgap list is one entry shorter. Wiring `KICK_TB` also needed mid-animation `ANI_SETPLYRMODE` (`MODE_INAIR2` for the leap, `MODE_NORMAL` on landing), so that is now a real (id, frame) table rather than the stomp-only special case batch 2 left behind -- which is also what lets `HEAD_HELD_STAND3` genuinely release `mode_headhold`, the entire point of that animation. Geometry grew 247 -> 264 real frames.
- **A pre-existing N64 link break, found by the same sync pass.** `src/generated/bret_defense.c` was added to the CMake host build in batch 1 but never to the N64 `Makefile`, so `wm_bret_block4_anim` had been an undefined symbol in the ROM build since then. Both it and the new `bret_grapple.c` are in the N64 source list now.
- **A real sourcing-parity defect, found by going through the remaining ids in order.** `HRTSEQ2.ASM` contains *two* different super-punch routines: `hrt_4_super_punch_anim` (:223), which `BRET.ASM:213 #scrt_cut` selects for the supercut secret move, and `hrt_2/4_super_punch2_anim` (:618/:677), which `#spunch_slap`'s own `FACE24 hrt,super_punch2_anim` selects -- and `FACE24` (`MACROS.H:51`) expands to exactly the `_2_`/`_4_` pair on the `MOVE_UP` facing bit, which is what `do_super_punch` reaches in ordinary play. An earlier cycle merged the two on the belief they named the same routine, deleted `WM_BRET_ANIM_SUPER_PUNCH4`, and asserted `SUPER_PUNCH2_2` permanently unmappable because "no `hrt_2_super_punch_anim` exists" -- true of that spelling, but the actual `hrt_2_super_punch2_anim` was there all along. They are different animations with different attack boxes (`AMODE_UPRCUT,-6,40,64,90` at frame 5 versus `AMODE_URN,19,75,35,24` at frame 3), so every ordinary super punch had been firing the supercut's box. Separated again: `SUPER_PUNCH4` is restored and holds `hrt_4_super_punch_anim` for the supercut, and the `SUPER_PUNCH2_*` pair now resolves to its own real extracted `super_punch2` data and window.
- **A dangling `if` in the attract state machine.** `app.c`'s `AMODE_LOOPS < 2` music guard (`ATTRACT.ASM:669 TURN_SOUNDS_OFF_IF_NEED`) had no body of its own and captured the following `break`, so the DCS command was sent unconditionally and, from the third attract loop onward, `WM_ATTRACT_DCS_LOGO` fell straight through into `WM_ATTRACT_SHOW_SPORTS_LOGO`'s entry body and ran it while the call was still `DCS_LOGO`. The source's gate suppresses *sound*; it never skips or re-enters a call. GCC had been flagging this as `-Wimplicit-fallthrough`; the warning is gone and the regression test was confirmed to fail against the original code, not just pass against the fix.
- **Routine chaining: the extractor now follows animations that don't end where their text ends.** A `SUBR` body carrying no `ANI_END`/`ANI_REPEAT` does not stop where the source text stops -- execution runs straight on into the words that follow, which is either the next `SUBR` (`hrt_2_hair_pickup_anim` into `hrt_4_hair_pickup_anim`) or the label of the body's own trailing unconditional `ANI_GOTO` (`hrt_2_raise_arm_anim`'s `ANI_GOTO,#cont`, landing inside `hrt_4_raise_arm_anim`). `tools/wlanim.py` now follows both, and `tools/wlattack.py` shares the same walk order so the audit judges -- and the reported frame indices refer to -- exactly the stream that gets extracted. A routine is only as sliceable as everything it chains through, so blockers propagate across the chain. All 21 previously-shipped sequences come out byte-identical. Two latent extractor bugs surfaced doing this and are fixed: a local label sharing a line with its frame (`#cont<TAB>WL 5,H4SL4C+FR1`) had that frame silently dropped, and chain targets were resolved from the top of the file even though local labels like `#cont`/`#hit`/`#missed` are reused across dozens of routines -- which resolved `hrt_2_raise_arm_anim`'s `#cont` to an unrelated earlier routine and produced frames from the wrong animation entirely. Neither affected any shipped extraction (checked), but both would have on the very first chained one.
- **Two more animations wired, and they are ones that could not have been before.** `KNEE_TO_HEAD4` (what `mode_headheld`'s punch/kick/punchkick branches select) and `FAKE_HOLD3` (what the head-hold monitor selects when the last headhold was under 120 ticks ago) both end in an `ANI_GOTO` into a shared tail. Under their own labels alone they extract as 1 and 3 frames; chained, they are 8 and 6 -- and `KNEE_TO_HEAD4`'s real `ANI_ATTACK_ON,AMODE_KNEE,11,44,51,49` sits at frame 2, past the end of the fragment, so that window simply could not have existed before. `FAKE_HOLD3` is off the one-tick `MODE_UNINT` stopgap list too, leaving 7 ids on it.
- **`ANI_SET_RPTCOUNT` loops are real now, not flattened.** `ANIM.ASM`'s `RPT_COUNT` mechanism (`_ani_set_rptcount:3530`, `_ani_dec_rptcount:3552`, `_ani_if_rptcount:3257`) makes a span of an animation play several times before the stream continues past it. `wm_visual_sequence` carries that span as `loop_first`/`loop_last`/`loop_count` and the runtime honours it, so an `ANI_ATTACK_ON` **inside** the span genuinely re-fires once per pass -- which is what the source does, its attack sitting inside the loop body. Unrolling into the flat frame list was the tempting shortcut and would have been wrong: it bakes one fixed length into data whose real length isn't always fixed. `tools/wlanim.py` refuses, loudly, the three shapes it cannot represent rather than inventing a number -- a **negative** `ANI_SET_RPTCOUNT` means `RNDRNG0(-n)`, a count drawn at runtime (`hrt_4_raise_arm_anim`'s own `-4`); a count of `1000` is an until-something-stops-it loop, not a fixed-length animation; and an `ANI_IF_RPTCOUNT` that branches *forward* (`hrt_uppercuts_to_head_anim`) is a first pass plus a separate repeated block sharing one counter, which a single span cannot describe. Three ids are wired as a result: `PIN2`, `PIN4` (35 and 33 frames, ~1300 ticks each, no attack window) and `KNEES_TO_HEAD`, whose first `ANI_ATTACK_ON,AMODE_HEADKNEES,4,34,70,54` is inside its loop and its second, `4,54,70,34`, after it -- so it lands four attack windows per playthrough from two table rows. Geometry grew 267 -> 287 real frames.
- **`ANI_CHANGEANIM` is a terminator, and reading it as one unlocked eight animations at once.** `ANIM.ASM:1301 _ani_changeanim` overwrites `OANIPC` **and** `OANIBASE` with the target animation and never returns -- so the routine genuinely ends there and the target begins. The source says so itself: the `.word ANI_END` following an `ANI_CHANGEANIM` is commented out in 16 places across `HRTSEQ2`-`4`. Treating it as an ordinary mid-stream command made these routines read far longer and far messier than they are -- `hrt_fall_back_anim` came out as 55 frames with four transitions when it is 12 frames with one, and `hrt_flying_kick_anim` as 38 rather than 9 -- and that inflated reading was what made the whole group look like it needed conditional-path evaluation it does not. With the extractor and the audit both stopping there (and the frame indices `tools/wlattack.py` reports stopping there too, which they briefly did not), eight ids are wired: `BUTTS2`/`BUTTS4` (whose window sits inside an `ANI_SET_RPTCOUNT,3` span, so one table row lands three headbutts), `FLYING_KICK`, `TBUKL_LEAP`, `RUNNING_GROUND_PUNCH`, `COMBO_PUNCH` and `COMBO_KICK` (three pulses each) and `FALL_BACK`. The per-animation header-mode-bits table generalised at the same time, so `MODE_NOCOLLIS`, `MODE_NOCONFINE` and `MODE_NOGRAVITY` are now honoured where the real headers carry them, not just `MODE_OVERLAP`. Geometry grew 287 -> 320 real frames.
- **The `ANI_CHANGEANIM` hand-off, and a correction to how it was detected.** An animation that ends by *becoming* another now genuinely does: the target starts through the ordinary `change_anim` path, so it gets its own header commands, attack windows and mode bits rather than inheriting the previous animation's. `TBUKL_LEAP` chains twice for real -- leap, hit the ground face down, get up -- and `FALL_BACK` (what the death path already selects) now runs on into `FACEUP_GETUP` instead of freezing on its last frame. Four ids were added purely as transition targets (`FACEDOWN_GETUP`, `FACEUP_GETUP`, `FACEUP_GETUP2_4`, `HITONGROUND_FACEDOWN`); none is selected by any dispatcher branch, because nothing *chooses* a getup -- it is what the fall before it turns into. Geometry grew 320 -> 337 real frames. This also **corrected the previous cycle**: treating every `ANI_CHANGEANIM` as a terminator was too broad. It only replaces the terminator when nothing after it ends the routine any other way -- the shape the source writes with `.word ANI_END` commented out below it. `hrt_2_butts_anim` reaches its transition when its repeat loop runs out but still has a real `ANI_END` on the button-mash `#ex` path, and `hrt_facedown_getup_anim`'s sits inside an `ANI_IFNOTSTATUS` free-toss branch with the ordinary ending below it. Both had been truncated at a transition they do not unconditionally take -- `BUTTS2`/`BUTTS4` shipped one frame short as a result, now fixed -- and only genuinely unconditional transitions are taken.
- **The ROM build was broken, and now there is a test that says so.** The N64 target is built with libdragon, which is not present in every checkout, so nothing in CI ever compiled it -- and its source list in the `Makefile` had drifted from `CMakeLists.txt`. Six symbols' worth: `wm_arcade_confine.c`, `wm_arcade_drone_data.c`, `wm_arcade_joystat.c`, `wm_arcade_mode_dead.c` and `wrestler_backend.c` were all missing, on top of the `bret_defense.c` omission found earlier. The ROM could not have linked. A full ROM does need libdragon, but catching this class of drift does not: `tools/n64_link_check.py` compiles the ROM's *own* C source list for mips64 with an ordinary cross-compiler and does a relocatable link, then reports any symbol the list fails to define along with the file that defines it. All 94 present core files compile clean for the real target word size and endianness under `-Wall -Wextra` with zero warnings, and the link is clean. It runs as the `ctest` target `wm_n64_link` and skips itself when no cross-compiler is installed. Verified to fail, naming the right files, when a source is removed again. This does not mean the ROM *runs* -- only that it no longer fails to link, and that list regressions are now caught by `ctest` rather than by nobody.
- **Wired attacks actually move now, and the motion data is generated rather than typed.** Every attack in this port played its real frames and set its real attack box while standing perfectly still, because `ANIM.ASM`'s inline motion commands were never translated. `tools/wlcommands.py` now emits them per animation -- `ANI_OFFSET`, `ANI_SET_XVEL`/`YVEL`/`ZVEL`, `ANI_ZEROVELS`, `ANI_ZERO_XZVELS`, `ANI_MIN_YVEL`, `ANI_FRICTION` -- each with the 0-based index of the frame it precedes, and `src/core/anim_frame_commands.c` applies them with the real semantics (x negated when the facing bit is clear, `MIN_YVEL` never lowering, `ANI_OFFSET` an instant nudge that also flips with facing). 126 commands across 36 animations. Rows are keyed on the animation's own **source label**, not a port-side enum id, so the same generated table serves any wrestler whose sequences get extracted -- there is nothing Bret-specific in it. Operands are resolved rather than guessed: 16.16 hex and sums, `AM_ABS`/`AM_FACE_REL`/`AM_HIT_REL`/`AM_NEWFACE_REL` from `ANIM.EQU:167-170`, and scoped local equates like `#yoff` (redefined 57, 50 and 37 within `HRTSEQ2.ASM` alone) tracked as the walk passes them; anything unresolvable raises instead of emitting a number. This replaced a blanket "zero x and z velocity on every attack" approximation that was **wrong for any attack that leaps** -- `hrt_kick_TB_anim`'s header really sets `ANI_SET_YVEL,70000h` and `ANI_SET_XVEL,-20000h,AM_FACE_REL`, so the turnbuckle kick now leaps instead of standing still.
- **`ANI_ADD_MOVE` is a combo-meter command, not a movement one.** Earlier notes here described it as "a real per-frame velocity impulse, not translated" and grouped it with the motion commands. `ANIM.ASM:4247 _ani_add_move` reads its three operands and calls `LIFEBAR.ASM:750 ADD_TO_COMBO_COUNT` on `WHOIHIT` unless that wrestler is dead; it touches no velocity at all. Its operands (`MOVE_C_8` and friends) are move-identity bits for the combo meter's `COMBO_START` mask. Corrected here, and it moves the 13 occurrences out of the motion gap and into the combo system -- which is separately still dead in this port, since nothing sets `combo_count` yet.
- **Attacks cancel into a run, and `start_run_anim` is real.** `ANI_IFBUTTONS` (`ANIM.ASM:71`) means "if EVERY named button is currently held, this animation becomes the target one" -- and all 40 occurrences across `HRTSEQ2`-`4` are the same case, `PLAYER_PUNCH_VAL|PLAYER_KICK_VAL -> start_run_anim`: the run cancel out of an attack's opening frames. Generated alongside the motion commands and applied by the backend. Its target needed doing too: `WRESTLE2.ASM:3443 start_run_anim` has **no `WL` frames at all**, so it was never going to come out of a frame extractor -- it is a state-setup routine whose own `ANI_CODE #setup_run` picks the run direction (the stick's left/right if held, else the facing), clears `GETUP_TIME`/`USR_VAR1`/`RUN_TIME`, faces the way he runs, sets `DELAY_BUTNS`, enters `MODE RUNNING` and *then* selects the wrestler's own run animation from `#run_anims[WRESTLERNUM]`. `wm_arcade_start_run` translates that as a shared routine, because the source's is shared -- the only wrestler-specific part is that table lookup. `WM_BRET_ANIM_START_RUN` had been selected by `mode_normal`'s punch+kick action all along and done nothing; it works now. The source's rotate-first path is deliberately not translated: `#ok1`'s unconditional `jruc #dorun` makes the `jruc #contx` after it unreachable in the shipped code.
- **The rest of the self-contained animation commands.** Nine more `ANIM.ASM` opcodes that need no subsystem behind them, generated per animation like the motion ones: `ANI_SETSPEED` (`:38`), `ANI_STARTATTACK` (`:117`, setting `ATTACK_TYPE` and `ATTACK_TIME = round_tickcount + ticks`, defaulting to 30 when the operand is not positive), `ANI_FACEUP`/`ANI_FACEDOWN` (`:33`/`:34`), `ANI_SET_WRESTLER_XFLIP` (`:95`), `ANI_CLR_BUTCOUNT` (`:97`), `ANI_SAFE_TIME` (`:104`), `ANI_GRAVITY_ON` (`:15`) and `ANI_CLR_STATUS` (`:58`). 297 generated commands now. Two notes worth keeping: every `ANI_SETSPEED` across `HRTSEQ2`-`4` is `100h`, the identity rate, so it is faithful but inert today; and `ANI_STARTATTACK`'s `AT_*` operands resolve from `DAMAGE.EQU:174+` rather than being hand-numbered. Translating `ANI_SET_WRESTLER_XFLIP` immediately exposed a real defect in **two existing hit tests**: they left `facing_dir` memset to `0`, so the attacker faced nowhere, and once the sprite mirror became real his attack box mirrored away from the victim and the hit stopped landing. A real actor always has a facing; the tests now set one.
- With that, what remains untranslated inside the wired animations is **301 occurrences across 31 kinds**, and every one of them needs a system this port does not have yet rather than another opcode: `ANI_CODE` (65) needs a callback bridge, `ANI_IFNOTSTATUS`/`ANI_IFBLOCKED`/`ANI_SLIDE_BACK` (71) need the animation VM to become a real branching interpreter rather than a flat frame list, `ANI_WAITHITGND` (29) needs gravity and ground integration, `ANI_SHAKER`/`ANI_SHAKEALL`/`ANI_DEBRIS*` (33) need a renderer, and `LEAPATOPP`/`ANI_SET_ATTACH`/`ANI_DETACH` (30) need the paired-actor system.
- **A real animation interpreter, and proof it agrees with what shipped.** `wm/anim_program.h` executes an animation as the op stream `ANIM.ASM` runs -- taking `ANI_IFSTATUS`/`ANI_IFNOTSTATUS`/`ANI_IFBLOCKED`/`ANI_SLIDE_BACK`/`ANI_GOTO`/`ANI_IF_RPTCOUNT` branches, running the commands, yielding on frames -- with all 40 wired animations generated as 1308 ops. Driven tick for tick against the flat runtime it replaces, on the **connected-hit path** the two agree exactly on 9 of 10 checked animations, including `hrt_2_pin_anim`'s 1299-tick run through its `ANI_SET_RPTCOUNT` loop. That agreement is the point: the flat extractor always kept the hit path, so reproducing it is what shows the interpreter invents nothing. The tenth, `hrt_4_push_anim`, is right *not* to agree -- its `ANI_IFSTATUS` skips a 5-tick frame when the shove connected, and the flat list kept that frame unconditionally. On the **miss path** they diverge across the board, which is the whole point: a miss really does play fewer frames, because the branches skip the connected-hit ones, and no flat list can express that. Bret's punch has been playing its hit recovery on every miss for the entire life of this port.
- **The emitter reads the whole roster, not just Bret.** `tools/wlprogram.py` was written against Bret's sequence files; it now walks all nine wrestlers' and emits **1518 of the 1521 animations across the eight playable wrestlers (99.8%)**, no wrestler below 179. Five parser gaps were closed to get there, each of which had been multiplying by eight: a mode word can use either separator (`MODE_UNINT+MODE_NOAUTOFLIP` throughout ADMSEQ); a branch target can sit *before* the routine as well as after it; a tick count can be a product, so every wrestler's `*_zip_anim` one-minute hold (`60*60`, or `TSEC*60` against `DISPLAY.EQU:46`'s `TSEC=53`) evaluates instead of refusing; a branch target can be a file-scope label in column 0 rather than a `#local` (`SHNSEQ2.ASM:1744 getup_in_4`, `RZRSEQ3.ASM:288 missed_rug`, `SHNSEQ3.ASM:4005 last_hitx`, `BAMSEQ3.ASM:221 START_OF_BREAKER`), which the assembler resolves identically; and a mode operand naming the *other* file's `MODE_*` still assembles, so `RZRSEQ2.ASM:2203`'s `ANI_SETMODE,MODE_INAIR` is translated the way the shipped ROM runs it -- `PLYR.EQU`'s player-mode ordinal 2 landing on `ANIM.EQU`'s `02h` `MODE_INTURN` -- rather than as what the line looks like it meant. The three still refused are honest: each branches into a whole other `SUBR`, where a `GOTO` keeps the original `OANIBASE` and a `CHANGEANIM` would not, so modelling it as a hand-off would change what `ANI_REPEAT` restarts. Regenerating every one of Bret's tables after these changes produces byte-identical output, which is what shows the generalisation did not disturb the data the interpreter was validated against. Bringing the other seven wrestlers onto the animation VM is now a data-extraction job rather than a translation job.
- **The interpreter is now the runtime.** `wm_bret_backend_change_anim` starts a `wm_anim_exec` whenever the selected animation has a generated program, and `wm_bret_backend_tick` runs it -- so the attack boxes, the mode and player-mode writes, the velocity commands and the end all come from the ops, on whichever branch the animation actually took. The side tables it subsumes are **deleted**, not left to drift: `attack_windows` and its `(id, frame)` lookup, `anim_transitions`, `plyrmode_changes`, `anim_header_mode_bits`, `attack_sets_facing_on_start`, and the entire generated frame-command table with its runtime and header -- ~1,250 lines of table and lookup replaced by running the ops.
- **Three real behaviour corrections fell out of the switch**, each one a case a flat frame index could not express. `hrt_2/4_ground_punch_anim` write their follow-up *twice*, once for the hit and once for the miss, so the flat list carried three attack boxes where any real playthrough fires **two** -- the third was an artefact of splicing both branches end to end. `hrt_knees_to_head_anim`'s `ANI_SET_RPTCOUNT` span is gated on an `ANI_IFNOTSTATUS`: the knee repeats only while it keeps landing, where the flat list looped three times **on a whiff**. And `hrt_kick_TB_anim`'s header writes `ANI_SETFACING` *before* its `ANI_SET_XVEL,-20000h,AM_FACE_REL`, so the leap's direction follows `NEW_FACING_DIR` -- a motion-command side table plus a separate "attacks set facing on start" rule could only apply those in the port's order, not the source's. A missed turnbuckle leap now also becomes `hrt_hitonground_facedown_anim` through its own `ANI_CHANGEANIM`, instead of playing the connected-hit recovery.
- **Six of the other seven wrestlers animate.** `wm/wrestler_backend.h` had stated its own blocker plainly — *"their frame data has not been extracted and their attack windows have not been traced"* — and neither is true any more. The join needed nothing invented: Undertaker, Yokozuna, Shawn, Bam Bam, Doink and Lex have always selected animations through `change_anim_label` with the source's **own routine name**, and the program registry is keyed on exactly that, so the two sides already spoke the same language and the seam simply had nothing behind it. **All 228 labels those six dispatchers can select emit as programs, and all 228 resolve at runtime.** Each now plays its own real frames, out of its own artwork, with a real per-frame hurt box and its real attack boxes.
- Three pieces of plumbing carried it: `wlprogram.py` emits every wrestler into one `src/generated/anim_programs.c` (**269 programs** — 41 Bret + 228 roster) so there is a single `wm_anim_program_find`; the frame collector learned the program op form as well as the flat-table form, since a program-driven wrestler has no `wm_visual_sequence` to collect from; and the geometry bundler takes several `.LOD` files at once, *refusing* rather than resolving by luck if two disagree about a frame. Result: **1772 frame geometries across eight `.LOD` files, covering all 1635 frames the programs reference, none missing.**
- Two consequences worth stating: the six are now confined by `wm_arcade_confine_wrestler` exactly like Bret (it reads `OBJ_COLLX1/X2`, and they finally have a real one), and their `MODE_BLOCK` now leaves through the block animation's own `ANI_SETPLYRMODE,MODE_NORMAL` rather than the ungated stopgap. The generated files and symbols were renamed off Bret to match what they hold. **Razor is the one left** — he uses a typed-anim-id interface rather than labels, so he needs a ~62-row id→label map.
- **The puppet system is in — the first grapple this port has been able to run.** `ANIM.ASM:2681 _ani_superslave2` is what a throw, slam, suplex or grapple *is*: **one animation driving both wrestlers**. The attacker names his own frame inline and looks the victim's up in a table indexed by the **victim's** `WRESTLERNUM` — so the same hiptoss shows a different victim pose per wrestler, which is exactly why none of it could be approximated. **927 uses, 73 tables, 4,315 rows**, all extracted. The runtime verifies the links first as the source does (both must hold each other, so a broken grapple drives nobody), then computes the attach offset from both frames' own animation origins and widths — `get_mpart_offsets`/`get_mpart_xsize`, which this port already has as real per-frame geometry.
- **A grapple now has a beginning and an end, not just a middle.** `ANI_SET_ATTACH` makes the link both ways; `ANI_DETACH` breaks it — carefully: own side first, the victim's only if he really was pointing back, and a victim left in `MODE_PUPPET`/`PUPPET2`/`ATTACHED` is put `ONGROUND` rather than left puppeted with nobody driving. `MODE_HEADHELD` is deliberately *not* in that list — the source had it and commented it out: *"This was fucking up the shawn franknsteiner move from headhold! Forcing him to dive down too low!"* Plus `ANI_ATTACHZ`, `ANI_SETOPPMODE`/ `ANI_CLROPPMODE` (both behind the mutual-link check), `ANI_IMMOBILIZE` (*"don't immobilize blockers!"*), and `ANI_IFOPPMODE`, whose single operand encodes both tests — the negative form is the ones' complement.
- One interaction was load-bearing: **`ANI_SUPERSLAVE2` *is* a frame** — it sets `OANICNT` and stops — but carries no `FRAME+FRn` operand, so the flat extractor can't see one. A grapple built entirely from them looked frameless, and the new `_body_stop` terminator rule cut those routines off at their own first `ANI_END`. Both it and the emitter's "no frames" refusal now count a `SUPERSLAVE2` as the frame it is. Playable roster: **1513/1518 (99.7%)**, and the five refusals are honest — four branch into another whole `SUBR`, and `bam_rxn2_fsteiner_anim` is literally `SUBR` followed by `.word ANI_END`.
- **Three extraction traps, each of which produced wrong data rather than failing.** `#puppet_tbl` is a *local* label and HRTSEQ3 alone defines it **eight times**, so resolving by name gave every grapple in that file the first table — it resolves per use site now, forward, because the files place a table *after* the routines that use it. Nine `LWWW` rows write their offsets as arithmetic (`46-10`), and a strict integer match truncated the list at the first one — seven of those nine are the **first** row of a per-wrestler list, so that emptied seven whole wrestlers out of one table. And DNKSEQ3 writes a `.ref` before each row, read as end-of-list. An unparsed row now fails loudly. Verified: **all 927 uses index within every playable wrestler's rows**, and all 953 victim frames have real geometry.
- **Razor makes it all eight.** He is the one wrestler whose dispatcher selects by a typed animation id rather than by the source's routine name, so he needed a map — **63 rows, every one checked against RZRSEQ1-4.ASM**. Sixty were derived from the id name and confirmed against the real routine list; three could not be and were read out of the source: `UPPERCUT4` is `rzr_4_uprcut_anim` (`RAZOR.ASM:1565` loads *"uprcut"*, not "uppercut"), `KICK_TB` keeps the source's capital TB, and `START_RUN` is the shared `WRESTLE2.ASM` routine with no frames of its own. `FINISH1`/`FINISH2` are deliberately **unmapped**: their routines exist but are eight lines of commands ending in `ANI_END` behind a `.if NUM_RAZOR_FINISHES` guard, with no `WL` frames at all — there is no animation there in the original either. **330 programs, 2044 frame geometries.**
- **The generator was not deterministic — that was the real bug.** The checked-in `anim_programs.c` was never stale. `wlprogram`'s body-growth loop iterated a **set** of missing branch targets, and growing the body for one target can satisfy or move others — so which came first decided the result, and set order depends on `PYTHONHASHSEED`. `yok_heldheadbutt_rpt_anim` emitted **94 ops under one seed and 203 under another**. That misled two separate diagnoses before the cause turned up. Targets are now taken in the order the routine *branches* to them: measured across three seeds, **1 of 330 programs differed before, 0 after**, and the settled output is what was already committed.
- With that fixed, the real `_body_stop` wart could be fixed and its blast radius measured honestly. Its alias rule — a `SUBR` reached before any frame is an alias for the routine after it — is genuine, but ran away on a routine with *no frames anywhere*. The discriminator is whether the routine **terminates** before the next `SUBR`: an alias doesn't, a finished routine does. FINISEQ's frameless finishers now correctly report no frames instead of swallowing the file, and **none of the 330 programs changed**. Both are covered by regression tests.
- **`ANI_CODE` is a real op now, and the first 26% of it is translated.** `ANIM.ASM:1277 _ani_code` is the plainest opcode in the machine -- `move *a4+,a0,L / call a0` -- and the single biggest hole in the animation VM: **2303 uses** across the eight playable wrestlers, collapsing to **145 distinct routines**. `wlprogram.py` now emits each call as a `WM_AOP_CODE` carrying the routine's own name, so an untranslated one is a *named, countable* gap rather than a line the extractor discarded, and the interpreter gained a `wm_anim_env` for what those routines reach for (the opponent, `RNDRNG0`/`RNDPER`, `triple_sound`), wired through the match from the app that owns the RNG and the audio queue.
- **First group: DCSSOUND.ASM's leaf sound routines** — the ones that pick a real index out of a real table, by `RNDRNG0` (`HIT_THE_MAT`'s `{76h,77h,78h}` mat hits behind a fixed `0C1h`; `SMALL_BOUNCE`, `SMALL_RUN`, both flame tables), by `WRESTLERNUM` (`DO_WAIL`, `DO_CHOKE`, `DO_NONO`/`DO_OTHERNONO` — `DO_NONO` reads the number of the wrestler *attached* to this one, `DO_OTHERNONO` its own), or by `RPT_COUNT` (`DO_DOINK_SLAM`), plus `FIND_AND_KILL_ENDLESS`'s single `ENDLESS_SOUND` slot and the five shove taunts behind their `RNDPER(500)` and 60-tick `DUMMY_WAIT` lockout. **16 routines, 590 of the 2303 call sites.**
- **Now at 1219 of 2303 call sites (53%), 35 of 145 routines.** Two more groups landed: the rest of the DCSSOUND sound family (`DO_BLOCKED`, `MAKE_HIM_SCREAM`/`DO_SCREAM` — which differ only in *whose* voice, the wrestler just hit or himself — `DO_RAZOR_RUG_SPEECH`, `GOUGE_SOUND`, `CALL_BONE_BREAK`), and the **self-contained actor-state routines**: `ckzpos` (86 — slide toward mid-ring when falling near the ropes so the opponent can walk around him), `no_bk_xvel` (70 — kill facing-relative backward velocity), `tbukl_flip`/`face_inside` (72 — which way to face on a turnbuckle, inverted for Doink and Yoko because *"doink is the opposite... so is yoko"*), Doink's buzzer-flash palette set, `am_I_dead`, `choose_2or4`, and `free_toss_check`/`setup_freetoss` (82).
- **Two defects in that first pass, found and fixed.** The registry resolved every routine by bare name — but a `#`-prefixed target is a *local* label scoped to its own file, and **22 of the 59 local targets are defined in more than one file, with different bodies**. `#make_black` has **seven distinct bodies across eight files**, because (as the source says at every copy) *"this is a black color within the wrestler's pal. It is different for each wrestler."* The shipped lookup had been handing Doink's black to six other wrestlers. Rows are now keyed on `(name, file)`. Separately, `PLYR.EQU`'s `INRING` is documented as *"0 = in ring, 1 = outside"* but this port stores the ordinary boolean — so `tbukl_flip`, translated with the source's polarity, answered backwards. The field now carries a comment saying the test flips.
- Six more global routines: `SET_DIR_FACE`, `set_tbukl_airmode`, `check_raisearm_bit` (whose sense is *inverted* from its name — the bit being set **clears** `MODE_STATUS`, so the celebration forks in only once), `clear_opp_counts`, `head_grab_time`, `halve_bk_xvel`. **The bridge is at 1273 of 2303 call sites (55%), 41 registry rows.** What's left is a 91-routine / 390-use tail plus four systems: the announcer speech queue, the combo message and award display, the crowd, and `WRSNDX`'s per-wrestler sound tables.
- **The puppet group is finished: `ANI_SETOPPVELS`, `ANI_DAMAGEOPP`, `ANI_SLAVEANIM`.** The first two act on the wrestler you are holding — `ANIM.ASM:3997` writes his three velocities, `:2175` takes his life — and both go through the same mutual-link check as the rest of the group, so a broken grapple hurts nobody. The third is the interesting one: `ANI_SLAVEANIM` does not *pose* the victim, it hands him **a whole animation of his own to run**, chosen out of a nine-slot table by his own `WRESTLERNUM`. A slam makes each wrestler play his own landing — Razor `rzr_slamnobounce_anim`, Yokozuna `yok_slamnobounce_anim`, Doink `dnk_slamnobounce_anim` — which is why it cannot be approximated by a pose. **95 tables extracted, 44/20/44 uses emitted.**
- **Naming an animation the port cannot play is a hole, so every one of them is now emitted.** The slave tables name **195 distinct animations**, and the generated program set is read out of the tables (`--slave-targets`) rather than listed by hand, so the two cannot drift. `src/generated/anim_programs.c` goes from **331 programs to 518**, and `frame_geometry.c` from 2,850 to **3,154** — every frame all 518 reference, none missing.
- **Three extraction rules had to become real to get there, each read off the source rather than assumed.** (1) *An animation is not always behind a `SUBR`.* `UNDSEQ3.ASM:876-1046` is eight choking animations named by plain column-0 labels, one after another, none ending in `ANI_END` — each ends in an `ANI_CHANGEANIM`, so the only boundary the source gives is the next label, and inside them every branch target is a `#local`. (2) *A frameless animation can be real.* The emitter refused any routine with no frames, as a proxy for a runaway span — but `ANIM.ASM`'s `wres_slave_anim` is four commands that park a wrestler while his attacker's animation poses him, and `WRESTLE2.ASM`'s `start_run_flung` is an offset and a getup timer. The honest discriminator is whether the body reaches its **own** `ANI_END`; a runaway one never does. FINISEQ's frameless finishers now emit their real seven commands instead of being refused, and the regression test checks the op stream rather than the refusal. (3) *Not every `.ASM` in the drop is in the game.* `WRESTLE.CMD` is the linker command file, and `ADMSEQ1-3.ASM` (Adam Bomb, cut from the roster), `REFSEQ1.ASM` and the superseded `HRTSEQ.ASM`/`YOKSEQ.ASM` are not in it — while `ADMSEQ3.ASM:199` defines `dnk_3_head_held_anim`, the same global `DNKSEQ3.ASM:1624` defines. Searching the directory alphabetically found Adam's. Label resolution now goes through the link.
- **A WIMP name field is eight characters, and one `.LOD` name is longer.** `BAM.LOD` lists `BURNBODY01`..`BURNBODY05`; `bam_jms.img` stores five images all called `BURNBODY`. The `.LOD`'s order *is* the container's packing order — checked here, 76 names against 76 images, the five truncations the only mismatch — so the nth full name is the nth image. The bundler resolves them that way and only when the counts agree, rather than refusing a frame the game plainly draws. Playable roster: **1516/1521 (99.7%)**.

- **Gravity, and the ground under it.** Until now every velocity this port wrote went nowhere. The reaction code has always set real launch velocities straight out of the source — `victim->y_vel = FX16(6)` for a body slam, `attacker->y_vel = FX16(4)` for a leap — and the animation VM has always run `ANI_SET_YVEL`/`ANI_ZEROVELS`. **Nothing added a velocity to a position.** What stood in for it was `wm_integrate_position`, a placeholder moving X and Z, documented in its own header as *"NOT a source routine"*. The real one is `WRESTLE2.ASM:2282 wrestler_veladd`, and it is translated now — X, Y and Z, the ground clamp with its `MODE_GHOST`/mutual-attach exception, `calc_ground_y` (`:2385`, which also writes the sprite's draw priority: 112 in the ring, 103 or 117 outside depending on Z), and gravity with `MAX_YVEL` as a **floor on falling speed, not a ceiling**. `WRESTLE.ASM:2456 wrestler_friction` came with it. The placeholder is deleted, and the real routine runs at the source's own place in the main loop — `count_button_presses` → **`wrestler_veladd`** → `wrestler_friction` → `animate_wrestler` → `confine_wrestler` — so the animation and the confinement both see the position the velocities just produced.
- The sign convention was read off the source rather than assumed: **larger Y is higher**. `ANIM.ASM:890` tests `OBJ_YPOSINT > GROUND_Y` for "still in the air", and its comment on `OBJ_YVEL … jrp #no_gnd` reads *"must have down velocity"* — so a negative `OBJ_YVEL` falls, gravity subtracts, and `MAX_YVEL = -1000000h` is the fastest a wrestler may fall. The port's own code already assumed exactly this (`y_int - ground_y` is read as height in React1/2/4/5/9 and in all eight dispatchers' `HEADHELD` case); it had simply never been driven. Wrestlers are now created standing on `MAT_Y` the way `WRESTLE.ASM:2724-2728` creates them, which that placement code does with the veladd clamp inlined — the source's own `;From veladd` comment.
- **`ANI_WAITHITGND` is the payoff: 767 uses, the single most common untranslated opcode in the machine.** It is not an instant command — it holds the frame that is showing and re-tests every tick until the wrestler is on the ground, then plays `SMALL_BOUNCE` and runs on. That is how every knockdown, slam and throw waits for its landing. One rule in it is load-bearing: while `MODE_KEEPATTACHED` holds a live grapple and the victim is not `MODE_GHOST`, it is the **victim** hitting the ground that ends the wait — a slam ends when the man being slammed lands, not when the slammer does. With it, `hrt_kick_TB_anim` is a real turnbuckle leap: he rises from the mat to a peak 52 units up, holds the airborne kick frame for the whole descent, and lands 30 ticks later — and *that* is when its `ANI_SETPLYRMODE,MODE_NORMAL` fires, which is what the test beside it had always claimed ("once he lands") and never actually exercised. `ANI_BOUNCE` (`:950`), `ANI_WAITHITOPP` (`:2300`) and `ANI_SETLONG` (`:3913`) landed with it — **1,220 more command occurrences emitted, 82.6% → 87.5% of every `ANI_*` across the eight playable wrestlers.**
- Three findings worth keeping. `ANI_WAITHITOPP` is not a command with operands at all — the source's own note is *"just like an ordinary WL ticks,frame type command except that the ANICNT is zeroed if we hit the opponent"* — so one source line is a mode set plus an ordinary frame, and landing cuts the hold short through `wrestler_veladd`'s own `#set_yvel`. `ANI_SETLONG` names a raw process field, and across the whole roster it names exactly two (`OBJ_GRAVITY` 115 uses, `DEBRIS_X` 148); a third would be a silent write to whatever sits at that offset, so anything else refuses. And **gravity is per-animation**: every `change_anim` resets `OBJ_GRAVITY` to `GAME.EQU`'s `GRAVITY`, which is what lets `hrt_flyout_anim` give itself a heavier fall (`0E000h`) for the drop out of the ring and put the default back afterwards — without that reset one heavy fall would leave the wrestler heavy for the rest of the match.
- One coupling had to be fixed to make any of it observable: the flat `wm_visual` track beside each program used to run on its own clock, and it could never keep up once a program could wait — a fall parks the program on one frame for as long as it takes to land, and the flat list has no such command in it, so the two drifted apart by the length of every fall. The flat track now steps when the **program** steps, one frame for one frame. Matching on the frame *name* was tried first and is wrong: `hrt_2_punch_anim` shows `H2PL3B01` twice in a row and `H2PL3B02` twice in a row, so a name search finds the frame the track is already on and never leaves it.

- **A knocked-down wrestler can now get up, and the whole loop is real.** `ANI_WAITROLL` (`ANIM.ASM:2990`, 171 uses) is the other half of the ground system: it parks the animation, forces `MODE_ONGROUND` *"just to be safe"*, waits out `IMMOBILIZE_TIME` and `GETUP_TIME`, and then calls `WRESTLE2.ASM:1290 do_roll` — and it is do_roll answering **"did not roll"** that lets the animation move on. Letting go of the stick is what gets him up. `hrt_tossed_anim` runs end to end now: thrown and launched, `ANI_WAITHITGND` for the landing, `ANI_GETUP,STAY_TIME`'s 270 ticks on the mat, the roll, then `ANI_CHANGEANIM,hrt_faceup_getup_anim`.
- **`do_roll` and its eight per-wrestler tables** (`tools/wlroll.py` → `src/generated/roll_frames.c`). Each is a speed, a Z velocity and an inverse multiplier followed by a frame list, and `do_roll` runs an 8-bit `ROLL_POS` through the multiplier to pick the frame. The lists are **not** in ascending order — Bret's runs `FR1`, then `FR13` down to `FR2`, because that is the direction his artwork rolls — and holding up rather than down negates both the advance and the Z velocity, so the frames run the other way too. `Z_BOUND` stops him once he is within 6 of it. One detail preserved rather than tidied: the multipliers are written as expressions like `10000h*12/255` and the assembler's divide **truncates**, so Bret's comes out 3084, the largest index 255 can reach is 11, and his thirteenth frame is unreachable in the shipped game.
- **The getup meter** (`WRESTLE.ASM:2538`), which is what makes any of that playable: `GETUP_TIME` counts down one a tick, and **any button or stick press — or its release edge — takes three more off**. That is the mash-to-get-up mechanic; the source's own comment is *"As long as getup_time has a value, he is stuck."* Reaching zero clears `PLYR_DIZZY` and `STARS_FLAG`. With it, a 270-tick stay-down becomes ~69 ticks of mashing. `ANI_GETUP` (`:52`) is what sets it, and it refuses while `PLYR_DIZZY` is set. This also closes a loop `wm_arcade_lifebar.h` had flagged: `adjust_health`'s `#will_die` path defers a death by setting `i_will_die` instead of killing outright, and the header noted it *"needs an eventual consumer this port doesn't have"* — `ANI_WAITROLL` is that consumer, and it calls `clear_lifebar` when the debt comes due.
- `ANI_ROT` (`:4441`) came with it — *"just sit and do nothing"*, the source's own comment: it holds the current frame and never advances, which is how `xxx_dead_anim` keeps a dead wrestler down. It is a **terminator for span purposes** even though it is not an `ANI_END`, which is what lets `xxx_dead_anim` be emitted at all (it is four commands and a rot, with no frames). It had to be, because `ANI_WAITROLL` hands off to it. **519 programs, 88.3% of every `ANI_*` across the eight playable wrestlers.**
- **A defect shipped in the gravity commit, found and fixed here.** `wlprogram`'s `MOTION_OPS` — the set deciding which ops get their operands written into the generated C — was a **hand-kept list**, so an op added to the command table but not to that set fell through to the no-operand default and had its operands silently emitted as zero. `ANI_BOUNCE`'s upward kick became 0 and `ANI_GETUP`'s stay time became 0. Neither test caught it: the bounce test checked the extractor's tuple rather than the emitted C, and the fall test passed on the *first* launch, which comes from a different opcode. The set is **derived from the command table** now, and a test checks both the derivation and the two real values in the generated file.

- **The combo meter, and a damage rule that was correct and unreachable.** Two counters, easy to confuse: `COMBO_COUNT` is per wrestler, bumped by `ANI_INC_COMBO` (183 uses), and it is what `adjust_health` reads — a hit landed while it is non-zero does **fixed** damage of `-max(10-COMBO_COUNT, 4)` instead of the normal scaled amount. `wm_arcade_lifebar.h` had said so in its own notes: *"nothing yet increments combo_count anywhere."* `PLT_COMBO_SIZE` is per player, grown by `ANI_ADD_MOVE` (273) through `LIFEBAR.ASM:750 ADD_TO_COMBO_COUNT`, and it is the meter **bar**, tripping a super flash at 16. `ANI_CLEAR_COMBO` (208) is **both ends** of a combo depending on whether the count was already set: starting one gives the victim an 80-tick immobilize the source labels *"Time opponent has to execute combo breaker"*; ending one holds his getup meter off for ten seconds. `hrt_combo_punch_anim` now runs a real six-hit combo end to end.
- One quirk recorded rather than tidied: `ADD_TO_COMBO_COUNT`'s own comment is *"Adds first value each time! [why?]"* — and the reason is that its "already added once" branch only skips re-setting `a3` from `a5`, which `movk 1,a5` has just made 1 as well. The bar grows by exactly 1 every call whether or not the move was already counted, and the `COMBO_START` bookkeeping affects nothing but `COMBO_START`.
- **A prerequisite that was a live defect of its own.** `WRESTLE.ASM:2503`'s five per-wrestler countdown timers — `DELAY_BUTNS`, `SAFE_TIME`, `DELAY_METER`, `IMMOBILIZE_TIME`, `WALK_FAST` — were being **set** all over this port (React1-5, the special moves, the attach opcodes) and counted down by **nothing**. An immobilize lasted the rest of the match, and `wm_arcade_try_attack_hit` rejected an immobilized attacker forever. They tick now, at the source's own place in the loop, each `jrz` guarded so it stops at zero — and `WALK_FAST` additionally guarded on being *positive* (`jrn #skp6`), because it is written negative elsewhere as a flag.

- **Sound, ropes, and the last opponent commands.** `ANI_SOUND` (104 uses) passes the sound's own index into `triple_sound` through the same env seam as everything else, with `run_snd` debounced against `FOOT_PCNT` — *"if there's been another foot noise from this guy within the last 12 ticks, give it a miss"* — and the negative-operand `wrtable_sound` branch confirmed **unreached** across the whole playable roster. Symbol resolution learned two things for it: `SOUND.H` joins the `.EQU` files, and lookups are case-insensitive, because the source writes `ANI_SOUND,run_snd` beside `ANI_SOUND,0C1h`. The rope commands are extracted — `ANI_BOUNCEROPE`, `ANI_BENDROPE`, `ANI_ROPE_Z` — each picking its bank from which side of `RING_X_CENTER` the wrestler is on, and a **negative selector picks the release variant** (16 `ANI_BENDROPE,-1` and 9 `ANI_BOUNCEROPE,-1`, so the branch is real). Worth noting: `:35`/`:37` use `jrle` and `:41` uses `jrgt`, so the boundary case falls opposite ways — kept as written. Plus `ANI_CLEAR_CLIMB`, `ANI_XFLIP_OPP`, `ANI_SETOPPFACING`, `ANI_SET_OPP_XVEL` and `ANI_PAUSE`.
- **The rope opcodes are extracted but not wired, and the reason is a real conflict.** This port carries **two independent rope translations** — `wm/ropes.h`'s `wm_rope_group` and `wm/arcade/wmania_rope_command.h`'s `WmRopeBank` — and both define `WM_ROPE_FRONT`. They have never collided because nothing included both. The arcade one has the whole `ROPES.ASM` script corpus and a live runtime behind it, and is the one these commands belong to, but joining it to the match means reconciling the two headers first. The opcodes carry their real operands and reach a seam that is defined in `wm_anim_env` — a named, countable gap rather than a dropped line.
- **Where the emitter stands: 93.6% of all 24,613 `ANI_*` occurrences** across the eight playable wrestlers. Of the 1,568 left, **1,135 are the renderer group** (`SHAKER`, `SHAKEALL`, `DEBRIS`, `ATTCHIMAGE`, `DRAW_NAME`, `SHADOWTRAIL`, `SCROLL_CTRL`, `DEBRISAT`, `SHAKECORNER`, `SHAKEROPES`, `START_DIZZY`) and need a renderer this port does not have. The other **433** are `ANI_CREATEPROC` (185, a process-spawn seam), `ANI_SET_IDIOT` (42, whose `allow_offscrn` global gates a `keep_onscreen` that is itself untranslated, so there is no consumer to write to), `ANI_TARGET`/`ANI_LEAPATPOS` (49, leap-at targeting), `ANI_IFROPE`/`ANI_IFNOTROPE` (40, rope proximity), `ANI_XFLIP_TBL`/`ANI_OPPOFFSET` (62, needing per-wrestler tables extracted the way the puppet and roll tables were), and a 55-occurrence tail across 8 kinds.

- **The VM's tail is closed.** `ANI_SETFLAG`, `ANI_CHECKWORD`, `ANI_IFOPP`, `ANI_FACE_OPP`, `ANI_HMBWAIT`, `ANI_WAITRELEASE` and `ANI_IF_RPTCOUNT_GE` — the last opcodes needing no subsystem. Two are worth calling out. `ANI_IFOPP` takes a **variable-length, -1-terminated** wrestler list and walks `CLOSEST_NUM`, not `ATTACH_PROC`: it asks who he is *fighting*, not who he is *holding*. `ANI_HMBWAIT` is a **three-way** frame hold — one tick count for a hit, another for a miss, a third for a block, blocked checked first, and a zero falls straight through instead of holding.
- **Two things fell out of that.** `ANI_WAITRELEASE` becoming real let a special case go: the Bret backend had been holding both animation tracks by hand whenever the block button stayed down, because `hrt_4_block_anim`'s `ANI_WAITRELEASE` was the one opcode the port could not express. The program parks itself now, so the hand-rolled version is **deleted** rather than left beside the op that replaced it. And `hrt_2_kick_anim` stopped matching its flat frame list on the hit path — correctly: its `ANI_HMBWAIT,7,0,0` holds the connected kick's last frame for **seven more ticks** and does nothing on a miss. The flat extractor had no way to represent a three-way hold, so it dropped the line, and every landed kick ran its recovery seven ticks short.
- **The three per-wrestler tables the VM indexes are extracted**: `ANI_CHANGEANIM_TBL` (hand off to the animation the running wrestler's *own* number selects), `ANI_XFLIP_TBL` (flip the *held* wrestler's sprite when his row says so) and `ANI_OPPOFFSET` (an x/y pair for him) — 4, 18 and 19 tables. They live alongside the puppet and slave tables and resolve the same way, **per use site, forward, across files**, because their labels are `#local` and reused just as freely: `#xflip_tbl` and `#release_table` each appear in several files with different contents. A test checks the resolution has not collapsed them into one. And like `ANI_SLAVEANIM` before it, `ANI_CHANGEANIM_TBL` names whole animations, so its 32 targets are emitted too — eight `xxx_liedown_anim` were missing. **527 programs.**
- **Where the VM stands: 94.1% of all 24,613 `ANI_*` occurrences.** Of the 1,463 left, **1,074 are the renderer group** and need a renderer this port does not have. The rest are `ANI_CREATEPROC` (185), `ANI_SET_IDIOT` (42, whose `allow_offscrn` global gates an untranslated `keep_onscreen`, so there is nothing to write to), leap-at targeting (53), rope proximity (40) and `ANI_LOOP` (8, round-end bookkeeping).

- **The ropes are joined, and a duplicate translation is gone.** This port carried **two** rope translations, both defining `WM_ROPE_FRONT`. `wm/ropes.h` was a 43-line stub whose own comment read *"TODO later: port command_table/sspring/dspring script selection exactly after the original table labels are converted into portable data IDs"* — and `wm/arcade/wmania_rope_command.h` is what then did exactly that, table routing, runtime and the complete `ROPES.ASM` script corpus. The stub is **deleted**, not reconciled. The four real banks live in `wm_match_state` now, initialised per bank the way `ROPES.ASM` creates one process each, ticked once per bank per match tick, and driven by the VM's own `ANI_BOUNCEROPE`/`ANI_BENDROPE`/`ANI_ROPE_Z`. `bam_climbthru_side_anim` bends the left rope through the real `dspr11_t`, `dspr23_t` and `dspr24_t` scripts, sets the rope Z high (`0x15a9`) and back, and releases it with `dspr_trans_t`.
- **The animation registry is complete.** Its contents had been a hand-picked list that grew one label at a time, every time a new opcode or table turned out to name an animation nothing had emitted — the slave targets, then the changeanim targets, then `xxx_dead_anim`. `--roster` now emits every animation the eight playable wrestlers own: **1,525 programs** against the previous 527, so `wm_anim_program_find` can no longer miss and that whole class of gap is closed.
- Two things surfaced from doing that, both worth keeping. **The original references two frames it has no artwork for**: `HRTSEQ3.ASM` names `H4HU4B+FR10` and `YOKSEQ1.ASM` names `Y2ST2Z+FR1`, and neither symbol is in any shipped `.LOD` — `BRET.LOD` has `H4HU4B01-04` and `07`, `YOKO.LOD` has `Y2ST2Z02-06`. The geometry bundler takes them **by name on the command line** rather than skipping missing artwork silently, so a *new* dangling frame still fails the build. **4,859 frame geometries.** And one real ordering bug: `start_run_anim`'s own `ANI_CODE #setup_run` had been running only when the target animation had no program of its own — which was true right up until every animation had one. It belongs to *selecting* the run, not to the frames that follow, and runs either way now.

- **The `ANI_CODE` tail, and the file-scoping trap all the way through it.** Sixteen more routines take the bridge from **1,251 to 1,339 call sites (54% → 58%)** and from 40 to 56 of the 141 distinct routines — all the self-contained actor-arithmetic class, no subsystem behind any of them. Every row is keyed on `(name, file)` for a reason that is live in this batch: **`#set_zvel2` has three distinct bodies** (−5.0 for six wrestlers, −6.75 for Doink, −5.75 for the Undertaker), `#set_opp_y` has two, and `#check_xvel` and `#impact_sound` have two each. Resolving by bare name would hand six wrestlers another one's numbers — exactly the `#make_black` defect.
- Details worth keeping: `#set_zvel1` and `#ckspin` are two names for the same body (`MODE_STATUS` from whether `FACING_DIR`'s `MOVE_UP` bit is clear); `#merge_xvels` averages at a **quarter**, not a half (`sra 2`), pairing with `#store_opp_xvel` over the file's own `#opp_xvel` word; the throw routines push the victim **away from his own facing**, so facing right sends him left; `#holdup`'s cap is the source's *"Max time to hold up in air (*2 ticks)"* of 25; `NOT_IN_RING` is another place the inverted `INRING` polarity bites; and `hiptoss_delay`/`fling_delay` are one limiter over two separate `PCNT` stamps — *"This blocked fling attempt is too close (in terms of time) to most recent grab"*.
- Left with reasons rather than skipped: `#grnd_hit` (14 uses) needs `change_anim1a` on the victim through a per-wrestler `#hit_t` table, `#spunch_delay` (14) reads `royal_rumble`/`PSTATUS`/`NUM_OPPS` — tournament state this port has no equivalent of — and `#impact_sound` (38) is `WRSNDX`, the per-wrestler sound tables.

- **A defect in the first tail batch, and the guard for it.** All thirteen rows shipped unreachable: a `#`-prefixed label is file-scoped, the emitter carries the `#` into the generated op, and the lookup is a plain `strcmp` — so a row spelled `set_zvel2` never matches a call site spelling it `#set_zvel2`. Their unit tests passed because they called `wm_anim_code_run` with the un-prefixed name **directly**, testing the row rather than the wiring. The reported "58%" was wrong for the same reason: the measurement stripped the `#` from uses and matched a `#`-less registry pattern, which over-counted globals *and* hid `#make_black`'s eight rows. There is now a source-tool test that every registry row matches a real call site, spelled exactly, and that a file-scoped row is called from the file it names.
- **Second tail batch: `ANI_CODE` at 1,413 of 2,303 (61.4%), 63 of 145 names.** `#set_opp_xflip`, `fix_bnc_flip`, `#ckongrnd`, `#inc_loop`, `#zero_butn`, `set_position`. The file-scoping pattern holds throughout: `#inc_loop`'s cap is 3 everywhere except Doink's copy, which is 2; `#zero_butn` clears `powerp_dtime1` in Bam Bam's copy where the other three clear `punch_dtime1`.
- Two findings worth keeping. **`set_position` is misnamed** — every position write in it is commented out in the source, so it moves nobody; the only live effect is the palette pair, of which only the `MY_PAL` half is translated (`SKELETON_PAL` comes from `pal_getf` on a per-file palette symbol, and there is no palette system here to resolve one). And `#set_opp_xflip` reads through `ATTACH_PROC` with no null check — benign on the original hardware, guarded here.

- `triple_sound` itself is deliberately **not** translated: it's a four-channel priority mixer over `triple_sndtab`, so the sound's own index passes through the env callback unchanged into the port's existing audio command queue, and channel arbitration stays a DCSSOUND port's job. One limit stated rather than papered over: `DO_DOINK_SLAM`'s index can reach one past its own three-entry table, which in the ROM reads the first assembled word of the next routine — this port plays nothing there rather than inventing a value it cannot know.
- **The announcer's voice queue exists now.** Every routine in the announcer speech group ends the same way — `CREATE` a process, `SLEEP`, then `ADD_IF_SILENT`: *"say this, but only if the announcer has nothing else to say."* Without the queue that arbitration has nowhere to happen, which is why the whole group (`CALL_MISSES` alone is 177 call sites) was stated as a gap rather than translated. `wm/arcade/wm_arcade_announcer.h` translates `DCSSOUND.ASM`'s side of it: `RESET_VOICE_QUEUE`, `ADD_VOICE` with its real `OUT_OF_RANGE_SOUND` refusal, `IF_SILENT_ADD_VOICE`'s `CMP A1,A2 / JRNE NO_ADD` gate, and `ANNOUNCE_VOICE` draining one line per tick through the same `triple_sound` seam as every other sound, reporting which side of `:2880`'s `0E0h` split each line fell on. The queue is pumped from `wm_match_tick`, so it runs in a live match. **Its first real caller is in the VM:** `_ani_inc_combo_count`'s `CMPI 8,A0 / JRNE NO_BESERKER` asks for `HES_JUST_GONE_BERSERK` (`SOUND.EQU:136`) at exactly the eighth hit of a combo — a threshold this port reached and did nothing about until now.
- Three things about it are approximations, and are marked as such in the header rather than smoothed over. The **slot count** is not quite pinned by the source: `bssx ANNOUNCE_QUEUE,32*20` reads as twenty longs while the stores are word-sized, so the larger reading (40) is used; nothing observable depends on it. There is **no sound-completion signal** in this port, so `ANNOUNCE_VOICE`'s play-and-`PRCSLP`-until-done becomes a caller-set `busy_ticks` hold, `0` meaning back-to-back. And the ring **loses a full lap** rather than growing — two bare cursors with no count read a wrapped-onto-itself write cursor as empty, which is exactly what the source does, since it has no overflow check either. The test asserts that behaviour because it is the original's, not because it is good.
- **And the announcer actually says things now: `ANI_CODE` 1,413 → 1,730 of 2,303 (61.4% → 75.1%), 63 → 70 names.** `CALL_MISSES` is the single biggest `ANI_CODE` routine in the game at 197 call sites, and it has no body: like its eleven siblings it is three lines — `CREATE` a `SOUND_PID` process, `SLEEP`, `ADD_IF_SILENT` with a table address and a percentage. So the routine *is* data, and `tools/wlvoice.py` reads all of it out of `DCSSOUND.ASM`: the twelve callers with their real sleeps and percentages (`MISSES` 350‰ after 5 ticks, `SPECIAL_MOVE` 550‰ after 10, `DROP_KICK` 400‰ after 15…), the thirteen line tables they draw from, `SET_UP_PERSONAL_CALL`'s five per-wrestler tables, and `ASCENDING_TABLE`. The names resolve out of the generated table rather than a hand-kept registry row, which is the same defect class that put thirteen unreachable rows in the tree earlier — here a caller cannot be spelled one way in the data and another way in the code.
- **`ADD_TO_QUEUE` itself is translated**, in the source's own order: the `.WORD -1` at `-050H` clearing `REPEAT_STATE`; the `RNDPER` gate; `RNDRNG0` over the row count at `-020H`; the six negative sentinels that mean "this row is a *personal* line" resolved from the attacking wrestler's number; `ARE_WE_REPEATING`'s four-line memory, and the forward walk it does when the row it drew was said too recently; then `ADD_VOICE` or `IF_SILENT_ADD_VOICE`, and the extra words of a two-line row until a zero. `REPEAT_MODE` works as written too — one row of it starts a counter that climbs `ASCENDING_TABLE` and, while it lives, *takes over* every announce call regardless of table or percentage, until `REPEAT_DUMMY`'s 80 ticks run out.
- Three things that came out of reading it closely. The tables carry their shape in three values written **before** the label, which `ADD_TO_QUEUE` reads at negative offsets, and the count is entries **minus one** because it is `RNDRNG0`'s inclusive maximum — read either wrong and the table silently draws the wrong rows, so the extractor checks each header against the data that follows it. The rows written *past* that count are not dead: the anti-repeat walk has **no bound at all** in the original (`SUBI 010H,A1 / ADD A3,A1` with nothing testing the end), so a rejected row near the end reads straight into them — and `FACE_HIT`, `MID_HIT`, `MISSES` and `MISS_YOKO` each put a line there that appears nowhere in the drawn range, meaning the only way the arcade ever says it is through a rejected draw. This port walks the same rows and then stops rather than reading past the data. And `PROC_MISSES`/`PROC_MISS_YOKO` never copy `WRESTLERNUM` into `A5`, while `MISS_YOKO` does contain a `GIVE_CREDIT` row: `GETPRC` (`MPROC.ASM:436`) carries only `A7`-`A12` into a created process, so **which wrestler the original credits there is not determined by the source** — this says nothing rather than picking one.
- Still not translated, and named: `DO_CROWD_ANYWAY` (the `.LONG` at `-040H` is a crowd reaction table, and this port has no crowd), and the announcer tables reached from the wrestler control layer rather than from an animation — `CLIMB_ROPES`/`JUMP_ROPES` from the turnbuckle code, `MATCH_OVER` and the seven `*_FINISHES` from the post-match speech. They parse cleanly; nothing here could check them, so they are not emitted.
- One pre-existing defect fixed on the way past: seven `ANI_CODE` registry rows stored a two-argument function in a three-argument function-pointer slot (`find_and_kill_endless`, `do_razor_rug_speech`, and the five shove taunts). GCC had been warning `-Wincompatible-pointer-types` about every one of them; calling through a mismatched pointer type is undefined, and it only worked because the ABI happened to tolerate it.

- **A combo now ends the way it does in the arcade: `ANI_CODE` 1,730 → 1,923 of 2,303 (75.1% → 83.5%).** `DO_COMBO_MESS` (`LIFEBAR.ASM:3687`, "Combo messages come from the scripts") is the single most-called `ANI_CODE` routine in the game — **193 uses across sixteen sequence files** — and it was a named gap because the line it says needs the announcer queue. It has one now. Translated as written: the `btst B_COMBO_BROKEN` early-out (which returns without even clearing `COMBO_START`, and the source says why — *"we audit it, but we don't adjust the bar or display any kind of message"*), the fact that **everything is decided on `COMBO_COUNT` minus one**, the `< 2` gate that skips the award and both sounds as well as the message (the source arguing with itself in a comment: *"How can you have a combo of 1 ?????"*), the regular-vs-ultra award split at 10, `triple_sound 0BAh` — *"0BAH humbug!"* — and `ADD_VOICE 01A4h`, which is `ADD_VOICE` and not `ADD_IF_SILENT`, so a finished combo talks *over* the announcer instead of waiting for him. Plus the display fudge that adds two to the number shown above five, *"for a better appearance"*.
- **And the award seam was dangling.** `wm_arcade_react.h` has declared `round_first_hit_award` since it was written, `AWARD.ASM`'s `round_award` is implemented as `wm_award_round_award`, and `wm_award_state` sits on the app — and **nothing connected them**, so `ANIM.ASM:2253`'s `RND_AWARD a13,FIRST_HIT_AWD` scored nothing at all. `JJXM.H`'s `RND_AWARD` macro is just `move :REG:,a0 / movi :AWARD_INDEX:,a10 / calla round_award`, so the seam is one callback: the app owns the arrays (they are per credit, not per match), the match hands it to both the animation VM and the hit path, which is exactly where the source calls it.
- **A second dangling `if`, same class as the attract one above, and this one had teeth.** `tick_gameplay`'s `if (a->call_ticks == 0)` guarded only `wm_match_start_attract`; `wm_app_bind_anim_env(app)` on the next line ran *every tick*. That was harmless while the bind only re-pointed stable pointers — but `wm_anim_code_reset()` inside it clears the source's timed sound globals, so once the announcer landed, a `CALL_x` process that sleeps 5-15 ticks before speaking was wiped before it could ever fire. Braced.
- Not translated, and named: the `AUD_COMBO` operator audit (there is no audit subsystem here), `is_there_one_already`'s `MESSAGE_FLAGS` gate and the `SPECIAL_MESSAGE` process (nothing here sets or clears those flags, so half a gate would be worse than saying so — and the gate has a quirk worth recording: its `NOT A1` makes it read the *other* side's slot), the royal-rumble branch that hands player 1's meter to player 0, and `zero_combo_meter`, which is lifebar object drawing. `HITCOMBO` turns out not to be a message id at all — it is a WIMP image symbol in `MISC.LOD`, the artwork itself — and `1803H` is the object's Z for `BEGINOBJ`, used the same way at `LIFEBAR.ASM:4130`.

- **Every wrestler grunts in his own voice now: `ANI_CODE` 1,923 → 1,983 of 2,303 (83.5% → 86.1%).** `impact_sound` (38 uses) and `DO_GRUNT` (22) are two lines of assembly each — all the behaviour is in `DCSSOUND.ASM`'s tables, and there are three layers of them. `MASTER_SOUND_TABLE` is a 9×59 word grid indexed `[WRESTLERNUM][move]`, with the source's own note on the layout: *"the first two are the noises a wrestler makes when he throws the move, and the second two are the noises he makes when he's hit with it."* A negative entry means `DEFLT` (`8000h`) — read `DEFAULT_SOUND_TABLE` instead. And what comes out does **not** go straight to `triple_sound`: `table_sound` (`DCSSOUND.ASM:1330`) tests `btst 12,a0`, and an index with bit 12 set names one of **67 random sub-tables** that `RNDRNG0` then draws from — which is why a wrestler's grunt is redrawn on every hit rather than fixed. `tools/wlwrsnd.py` extracts all of it.
- **The file-scoping trap again, and this time it decides which body 38 call sites get.** `MACROS.H:246` — `SUBR` emits a `.def` and is global; `SUBRP` does not and is file-local. `impact_sound` is `SUBR` in `YOKSEQ3.ASM` and `SUBRP` in `HRTSEQ4.ASM` and `RZRSEQ3.ASM`, and the bodies **differ**: the global one plays an extra `WRSNDX HDBUTT_L1,HDBUTT_L2` on the wrestler himself before the rug slam, the local ones only do the slam. So eleven of Bret's own animations call the Undertaker-file version *with* the headbutt sounds, and one calls Bret's own *without*. `DO_GRUNT` is the same shape: `SUBR` in `SHNSEQ2.ASM`, and a bare label in `LEXSEQ3.ASM` — also file-local — which uses `WRSND` with a **fixed** `W_LUGER` instead of reading `WRESTLERNUM`. Resolving either by bare name gets one of the two wrong for everybody.
- Three things the data itself says. **Four wrestlers reuse another's row verbatim** — the Undertaker, Shawn and Luger are byte-identical to Bret, and Adam Bomb to Yokozuna — which matters because a drifted 59-word split would look exactly like that, so the extractor counts each commented block independently and requires both readings to agree. **Nothing in the shipped grid is a zero**, so `WRSNDX`'s `jrz DONE?` on a wrestler's own entry is unreachable as written; the thirteen zeros are all in the default table, where they do mean silence — and `GRABFLING_T1`, the first of `DO_GRUNT`'s two sounds, is one of them, so the grunt is a single sound for every wrestler who defers there. And the random sub-tables **alias**: `#doink_kick_a`, `#doink_knee_a` and `#doink_slam_a` are three labels stacked on one `.word` line, so Doink's kick, knee and slam draw from the same three sounds.

- **Measuring what's left, and finding two defects by doing it.** The emitter silently skips any `ANI_*` it does not model — that is deliberate, and unlike the flat extractor it cannot corrupt frame order — but it meant the only answer to *"how much of the animation VM is missing"* was a hand-kept one, which is exactly how the `MOTION_OPS` defect shipped. `wlprogram.py --census` now counts them: **51,168 ops emitted, 2,102 skipped — 96.1%** across 1,524 programs, and it names the call sites. The remainder is overwhelmingly renderer (`ANI_SHAKER` 451, `ANI_SHAKEALL` 389, `ANI_DEBRIS` 197, `ANI_ATTCHIMAGE` 194). But the census also listed ops the emitter *claims* to handle — `ANI_GOTO` 7, `ANI_IFSTATUS` 4 — and a silently dropped branch is not a gap, it is a wrong animation.
- **A local label may begin with a digit.** Five do — `HRTSEQ4.ASM`'s `#4block` and `SHNSEQ3.ASM`'s `#4`, `#2`, `#4xc`, `#2xc`, named after the 2-count and 4-count pin they lead to — and the label pattern required a letter after the `#`. They were invisible on **both** sides: their definitions were not seen as labels, and the six branches to them were dropped. `SHNSEQ3`'s gsuplex animations are where that showed: `choose_2or4` sets `MODE_STATUS` to pick whether Shawn's victim lands face-up (a 2-count) or face-down (a 4-count), `ANI_IFSTATUS,#4` takes the face-down arm and `ANI_GOTO,#2` skips it — with both dropped, the animation ran through **both arms**, playing the face-up frame *and* the face-down frame and doing `ANI_FACEUP` then `ANI_FACEDOWN`. The `#4 WL 1,S4ST4C+FR1` line lost its frame outright too, because the label-strip that handles a label sharing a line with its frame failed the same way.
- **And an animation does not always start at op 0.** Fixing the labels surfaced the second one. A routine can branch *back* into shared code earlier in the file — every wrestler's `*_4_hitblock_anim` jumps into his `*_4_block_anim`'s hold at `#4block`, which is how getting hit while blocking returns you to blocking — and the emitter already grew the body backwards so the branch had a target. What it never recorded is that the routine's **own** first command is then some ops in. **42 of the 1,524 programs are like that**, and every one had been starting by playing code it merely jumps into: `hrt_4_hitblock_anim` opened on the block-hold frame instead of its own, and `bam_combo_pogo_anim`'s body starts **256 ops** into its stream. `wm_anim_program` carries an `entry` now and `wm_anim_exec_start` begins there. Both defects have regression tests that were confirmed to fail against the old code, not just pass against the new.

- **The end of a match is announced now.** `CALL_MATCH_OVER` (`DCSSOUND.ASM:3793`) is the one caller in the announcer family that is a *decision tree* rather than a table and a percentage, so its constants are read out of the source individually rather than guessed: the process's `SLEEPK 5`; a 20% `RNDPER` that hands the line to the **wrestler** instead of the announcer, out of his own `*_FINISHES` table; the winner's `p1winstreak` against `CMPI 4`, and another 20% for one of two over-four-wins lines chosen by a further `RNDPER 500`; and otherwise `ADD_TO_QUEUE` at **1000 per mille** — not `ADD_IF_SILENT`, so the end of a match always says something, over whatever the announcer was already saying. `xor a8,a9` picks the table: `MATCH_OVER` when a *player* lost (the source's own comment, *"Lawlor cheers/Vince gripes"*) and `MATCH_OVER_DL` when a drone did. That makes ten more of the extracted tables live, including all seven `*_FINISHES`.
- Three things this family does differently, all now recorded rather than smoothed. It has **no reset-repeat word of its own**: these tables were written with just a `.LONG crowd` and a `.WORD count,stride`, so the word `ADD_TO_QUEUE` reads at `-050H` is whatever happens to sit there — the last data word of the table before it, or, for `HART_FINISHES`, the low half of a `.LONG` in the `WHICH_WRESTLER_TALKS` pointer list above it. Accidental, but real, and it decides whether the call clears `REPEAT_STATE`. It also has **no walk-forward rows**: every row is drawable and nothing follows, so a wrestler with a single line who just said it fails `ARE_WE_REPEATING` and the original walks straight off the end into whatever the assembler put next — this port stops and says nothing. And **the Undertaker and Yokozuna win in silence**: their tables hold a single `0`, which is "no line", not line zero.
- `MATCH_OVER_DL`'s first row is `GIVE_CREDIT`, so what the announcer actually says there is the **loser's** personal line resolved through `SET_UP_PERSONAL_CALL` — the machinery that landed with the queue, doing real work now.

- **Five more self-contained routines: `ANI_CODE` 1,983 → 2,021 of 2,303 (86.1% → 87.8%), 78 of 145 names.** `set_xdrift` (14 uses), `set_buckoff_vels` (7), `skick_delay` (6), `tgt_ground` (6), `tgt_tbukl` (5) — each reads and writes only the actor it is given, plus ring geometry. `set_xdrift` is the source's own *"Leap at the center of ring!"*: in the ring, and only outside a `60h` deadband, a flat 3.0 of X velocity toward the middle. `set_buckoff_vels` is being thrown off the turnbuckle — pushed toward the middle in X and Z, popped 5.0 upward. `skick_delay` is a two-second lockout between super kicks, reported through `MODE_STATUS`, with the source's own comment on the refusal: *"This blocked fling attempt is too close (in terms of time) to most recent grab."*
- Four details worth keeping. `set_xdrift` measures from **`RING_X_MID` (1024), not `RING_X_CENTER` (1074)** — the two are 50 apart and it is easy to reach for the wrong one. `tgt_tbukl` aims at **`RING.ASM`'s** `vln_*_rope`, whose right-hand X is five units from `WRESTLE.ASM`'s `vln_*_rope_r` that the port already had, so both are kept as separate constants rather than folded together. `skick_delay` writes its `LAST_SKICK` stamp **before** the comparison, so a refused attempt still restarts the window. And its royal-rumble early-out skips the check entirely in a mode this port has no equivalent of — the flag is 0, so the check always applies, which is the shipped behaviour of every match this port can actually run.
- `TGT_XOFF`/`TGT_YOFF`/`TGT_ZOFF` are on the actor now. `ANIM.ASM:1428` is explicit that filling them is the caller's job — *"NB - user must set TGT_XOFF,YOFF & ZOFF <-- these are the actual target"* — so the routines that set them are translated ahead of `ANI_LEAPATPOS`, which reads them and is not.

- **An `ANI_CODE` routine can move the program counter, and now one does: `ANI_CODE` 2,021 → 2,037 of 2,303 (87.8% → 88.4%).** `HRTSEQ3.ASM:2896 #rope_check` (16 uses) is the roll-uppercut hitting the ropes, and it does something no other translated routine does: `movi #stand,a14 / move a14,*a13(ANIPC),L`. **`ANIPC` is a field on the wrestler, not state inside the interpreter** — so a routine sets it and the VM applies it the moment the routine returns, which is exactly how it is modelled here. Resolving a label at runtime needs the labels, so the emitter now exports them: every program carries its own `#name -> op index` table (4,571 labels across 1,524 programs, ~73 KB), and `wm_anim_program_label` looks one up. The rest of the routine is real too — `CAN_MOVE_DIR` masked with `MOVE_LEFT|MOVE_RIGHT` to ask "am I against a rope", the matching bank wobbled through the rope seam, and the `03Ch` thump.
- **And where it jumps is a bug in the original, kept as written.** `#rope_check` is a single `SUBRP` shared by `hrt_roll_uppercut_anim` *and* `hrt_combo_roll_uppercut_anim`. It always writes `#stand` — which is a label inside the **plain** animation. The combo one has its own tail, `#standc`, and that is the one that does `DO_COMBO_MESS` and `ANI_CLEAR_COMBO`. So a **combo** roll-uppercut that ends against the ropes runs the plain animation's ending: no combo message, no cleared count. `#standc` is defined once and branched to from nowhere — dead code in the shipped ROM, which the test asserts directly (`#stand` is not in the combo program at all, so the jump has to leave it). This port hands it over as a `become` of `hrt_roll_uppercut_anim` entered at `#stand`, with `ANIPC` surviving the hand-off, because that is what a flat address does on the real machine.

- **Three more, and Doink is different again: `ANI_CODE` 2,037 → 2,058 of 2,303 (88.4% → 89.4%), 82 of 145 names.** `MAYBE_BOUNCE_ROPE` (8 uses) is *"into the ropes!"*, but only sometimes — `MOVK 10,A0 / RNDRNG0 / MOVE A0,A0 / JRNZ` speaks only on a draw of exactly zero, and `RNDRNG0`'s maximum is **inclusive**, so that is one time in eleven rather than one in ten. It needed the announcer queue, which is why it waited. `#clrcnt` (5) is one clear, with the source's own note on the odd choice of field: *"We are re-using BUT_COUNT in the player process."*
- `#set_trgt` (8) is where to land when climbing out onto the apron, and it is **the file-scoping trap once more**: seven of the eight copies put it at `RING_X_CENTER ± (0f8h+60)`, and **Doink's is `0f8h+50`** — ten units further in. Resolving by bare name would put him on everyone else's apron. It is only translatable now because `TGT_XOFF`/`YOFF`/`ZOFF` landed with the last batch.
- One thing worth knowing about the RNG, found by writing the test for that one-in-eleven gate. `RAND` stirs from **`HCOUNT` and the stack pointer** — real entropy on the hardware, and *none at all* in a bare test: with no readers wired it settles on a single value and returns it forever. The app feeds it tick-derived versions of both, so the live game is fine, but a test that builds its own `WmRng` and expects variety gets a constant. The tests here drive it through the documented latched-input path instead, in the same shape the app uses.

- **The self-contained tail, cleared: `ANI_CODE` 2,058 → 2,095 of 2,303 (89.4% → 91.0%), 92 of 145 names.** Ten more routines, none of them needing a system behind it. `check_xvel` (8 uses, a global seven other files call) stops a toss that would carry the victim out over the ropes — heading further out, he is reversed to 1.5 inward and it reports through `MODE_STATUS`, with the source's own note on why: *"Will land on ropes"*. `inc_loop` (8) is the Undertaker's chokehold counter. `#reduce_dmg` (4) is `MACROS.H:714`'s `SPCDMG` — stamp `SPECIAL_DAMAGE_TIME` and arm `NEXT_DAMAGE`. Plus `#reattach`, `SET_OPP_GRAV_NORM`/`_LOW`, `target_whoihit`, `#set_opp_facing`, `#blocked_vels`.
- **Three of the ten differ per file, and the differences are all constants** — so they are the registry's `param` rather than three bodies. `#reduce_dmg` is `RD_PILEDRIVER` (22) over 80 ticks for Razor, `D_STOMP2` (6) and `D_PUNCH` (8) over 40 for Shawn and the Undertaker. `#go_high` — *"Go higher for last hit!"* — pops the opponent 5.0 for Doink and Bret and only 4.0 for Razor, and all three decline to lift Yokozuna at all, with a two-word explanation: *"Yoko too fat"*. Doink's copy also keeps a commented-out earlier version that read `ATTACH_PROC`, with the note *"Got rid of this because attach_proc is a zero after a hit"* — which is why all three read the closest wrestler instead.
- Left out of this batch on purpose: `#pause_opp` needs the opponent's `OANICNT`, the animation tick counter, which lives in his interpreter rather than on the wrestler — half of it (the `MODE_UNINT`) would be worse than none. `#flsh_wht` is `flash_white`, palette rendering.

- **Seven more singles: `ANI_CODE` 2,095 → 2,102 of 2,303 (91.0% → 91.3%), 99 of 145 names.** `#zero_x_4` (*"Don't float if dropping straight down"*), `#delay_whoihit`, `#ck_flip`, `#ck_dead_opp`, `#set_wrestler_xflip`, `#stop_dmg`, and `#no_bk_xvel` — which is **the file-scoping trap one last time**: the global `no_bk_xvel` kills only `OBJ_XVEL`, and `SHNSEQ3.ASM`'s local copy kills `OBJ_ZVEL` as well. Both are now registered, and a test asserts the difference directly. `#ck_flip` is worth a note too: its two halves test the sprite's flip bit with **opposite senses** (`jrz` on the left, `jrnz` on the right), which is exactly what makes each side end up facing into the ring.

- **The self-contained tail is finished: `ANI_CODE` 2,109 of 2,303 (91.6%), 104 of 145 names.** Five last routines — `#check_xvel` (a *different* routine from the global `check_xvel`: push him 2.0 the way he is facing, and kill the Z drift with it), `#grunt` (`WRSND W_SHAWN,...`, naming the wrestler outright like `LEXSEQ3`'s `DO_GRUNT`), `#hit_ground`, `#set`, and `#choose_2or4`. Two nice endings: `#set` turned out to be `#delay_whoihit`'s body with a different window (eight seconds against 55 ticks), so both share it with the window as data; and `YOKSEQ4`'s local `#choose_2or4` is **byte-identical** to `SHNSEQ4`'s global one, so for once a file-scoped copy shares the body rather than needing its own — with a test that asserts the two agree, in case that ever stops being true.
- **What was left at that point was 194 uses in 41 names, and every one of them blocked on a system rather than on reading more assembly.** Measured: 80 uses want the renderer (`create_impact*`, `start_smoke`, the salt and debris routines, palette flashes), 39 want round-end and pins (`win_announce`, `grnd_hit`), 24 want a crowd (`DO_CROWD_CHEER`), 21 want the roster sweep and `calc_line_x` (`set_tbukl_confine`, `hit_nearest`), 14 want tournament state (`spunch_delay`), and 7 want process creation. Two of those six are done below; `tools/ani_code_census.py` is the number, so it is reproducible rather than quoted.

- **An independent reading of the animation streams, and what it does and does not settle.** Everything in `tools/` shares `wlanim.py` — its lexer, its label patterns, its routine spans — so a mistake in that layer is invisible to every generator built on it, and the flat-extractor cross-check only ever had partial independence. `tools/wlverify.py` shares **nothing**: its own comment stripping, its own routine spans, its own label scoping, its own branch resolution, its own read of `WRESTLE.CMD`. And it **runs** the animation rather than re-extracting it, so what comes out is the frame sequence actually played — which checks the emitter and the interpreter's tick model together, end to end.
- **294 animations, all identical — frames and tick counts, zero divergences.** Both checks are confirmed to fail against a deliberately perturbed frame duration, and against one in an animation only the independent reader covers, so neither is passing by construction. It also caught 24 animations it could play that were never emitted; all 24 turned out to be **Adam Bomb**, the cut wrestler, in files `WRESTLE.CMD` never links — so the verifier now reads the linker's own object list, and a genuine missing program would fail the test.
- **The limit is the point, and it is a real one.** `wlverify.py` can only play bodies made of frames, `ANI_GOTO`, `ANI_REPEAT`, the repeat counter and `ANI_PAUSE`. Every conditional branch and every wait — `ANI_IFSTATUS`, `ANI_WAITHITGND`, `ANI_CHANGEANIM` and the rest — makes a body unverifiable, so **the interesting half of the VM is exactly what is excluded**: 192 of 1,524 programs, and by construction the simplest ones. This raises confidence in the frame and tick backbone. It says nothing about the branching semantics, and nothing at all about the arcade — there is still no reference to compare against. One caveat recorded in the tool: `ANI_SETSPEED` is treated as stream-neutral because this port's interpreter never reads it for tick counts, which means this check *cannot* tell whether the original scaled frame durations by it.

- **The `ANI_SETSPEED` question, settled — and it was a real hole.** The independent verifier had to treat `ANI_SETSPEED` as stream-neutral because this port's interpreter never read it, and I flagged that as unresolved rather than assume it. It is resolved now, from the source: `ANIM.ASM:151` genuinely **does** scale a frame's duration by it — `move *a13(ANI_SPEED),a1 / mpyu a0,a1 / srl 8,a1` — and then shifts the result again by `hyper_speed_on`, `AWARD.ASM`'s double-speed powerup. So `100h` is identity, and the port was silently dropping the whole mechanism.
- **Why nothing broke: every one of the 1,220 `ANI_SETSPEED` commands in the entire source drop is `100h`**, and `hyper_speed_on` is a powerup toggle that is 0 in normal play. The port was right by a fact about the *data*, not by construction — the exact shape of defect that has bitten repeatedly here. The scaling is implemented now, so it is right by construction instead, and the port gets the hyper-speed powerup for free. Only a **frame's** count scales: `ANI_PAUSE` (`:1252`) and `ANI_HMBWAIT` write `OANICNT` directly, and the tests assert that asymmetry rather than assuming it.

- **Widening the independent check, and a negative result worth keeping.** Extending `wlverify.py` to model conditional branches looked like the obvious next move; measuring first showed it would add **ten** animations (192 → 202), because the real blockers are the *waits* and `ANI_CHANGEANIM` (`ANI_WAITHITGND` alone blocks 537 bodies), not conditionals. So that plan was dropped before it was built.
- What did pay off was fixing two gaps in the **reader itself**, both found by comparing its frame lists against the emitter's across the whole roster: `SUBR` labels **alias** (`bam_stand2_anim` is immediately followed by `bam_stand8_anim` with one body under both, so the first was getting an empty body), and a frame's tick count is sometimes a small **expression** (`WL 2+1,B2WL1A+FR4`). Strict independent coverage went **192 → 294 animations**, still all agreeing exactly.
- **The check that was not shipped, and why.** A whole-roster frame-subsequence comparison reaches 92% agreement (1,339 of 1,455), and the residual 116 are not divergences: they are the two documented emitter behaviours — bodies grown across routine boundaries, and cross-routine branches like `bam_run2_anim`, whose entire body is `ANI_GOTO,#run2` into *another routine*. Making that check green would mean replicating the emitter's span logic inside the thing meant to check it independently. It is left as a measurement rather than a test, because a check that has to copy its subject is not a check.

- **The sprite bundler was NOT generalised, and the measurement is why.** Pointing it at the whole roster was the plan; costing it first killed it. Bret's bundle is 303 frames for **1.8 MB of payload** (5.8 KB a frame, 11.2 MB of generated C). The roster names **4,861** frames — about **29 MB uncompressed**, on top of an `anim_programs.c.o` that is already 3.5 MB. `tools/wimpimg.py` says why in its own header: the WIMP containers are the **artist format, before the arcade's own LOAD2/DMA2 packing**, so this is the unpacked floor and the real arcade ROM stored it packed. And an N64 has **4 MB of RDRAM** (8 with the Expansion Pak) — 29 MB cannot be resident under any packaging. Expanding every frame into a linked C array is the wrong shape; the art has to stay packed on cart and be DMA'd per frame, which is what the original did. That is a design decision, not a refactor, so it is written down rather than half-built.
- **A regression I introduced, found by the invariant sweep.** `xxx_dead_anim` is not part of the roster sweep — `scripts/regenerate_source_data.sh` adds it with an explicit `--animation WRESTLE2.ASM xxx_dead_anim`. Regenerating the file by hand with just `--roster --slave-targets`, which I had been doing, **silently dropped it**, and `anim_program.c`'s death path sets `exec->become = "xxx_dead_anim"` — which then resolved to nothing and ended the animation quietly. There is a test now that every hard-coded `become` target in the C resolves to a real program, confirmed to fail against exactly that mis-regeneration. `ANI_ROT` also joined the terminator set: it parks forever rather than ending, which is how `xxx_dead_anim` finishes.

- **Tournament state and the crowd, both real: `ANI_CODE` 2,109 -> 2,147 of 2,303 (91.6% -> 93.2%), 104 -> 106 names.** Two of the six systems the previous entry called blockers turned out to be answerable from the source rather than from a subsystem this port lacks.
- **`spunch_delay` (14 uses) needed a gate, not a tournament.** `DNKSEQ3.ASM:2256` is `skick_delay`'s twin — the same two-second super-punch lockout reported through `MODE_STATUS` — behind three lines of match configuration: `move @royal_rumble,a0 / jrnz #ok`, then `@PSTATUS` against 3 (two humans), then `@NUM_OPPS` against 1. The interesting part is that `skick_delay` has this same gate *commented out* in the source, so the two were written together and one had it disabled. `wm_anim_env` now carries `royal_rumble`, `pstatus` and `num_opps` and the routine **evaluates** them; this port only ever starts the `PSTATUS==0` attract match or the single-human `#1plyr` one and only ever creates one opponent, so the gate resolves to "check" today — but it resolves by reading the values, so a match type added later gets the source's own behaviour instead of an assumption baked into a comment.
- **`DO_CROWD_CHEER` (24 uses) is two instructions, and the crowd tables are data.** `CROWD.ASM:1119` is `MOVK C_OVERIDE|C_LONG,A3 / CLR A4` falling straight into `crowd_cheer` — so the routine *is* its arguments, and `crowd_cheer` itself walks `NUMCROWD` members of `CROWDDATA` swapping each one's `SCPTR` for its `CHEER1`/`CHEER2` script. That is a sprite subsystem this port has not got, so it is reached through `wm_anim_env` exactly like `triple_sound` and the rope commands, with `GAME.EQU:299-306`'s real flag bits.
- **Which also closed a hole I had written down and left open.** `ADD_TO_QUEUE`'s `MOVE *A2(-040H),A3,L / JRZ NO_CROWD / CALLA DO_CROWD_ANYWAY` had been translated as a comment saying the port has no crowd. It has one now: `tools/wlvoice.py` extracts the **eight** crowd tables at `DCSSOUND.ASM:4443` (a one-word header and four words a row — sound, duration, flags, `RNDPER` value — which is why `DO_CROWD_ANYWAY` picks a row with `SLL 6,A0`), and `wm_announce_table` carries the `.LONG` that names one. So the announcer's own reactions genuinely fire: `MISSES` -> `CROWD_FAIL`, `SETUP_MOVE` -> `SETUP_TABLE`, `CLIMB_ROPES` -> `CRESCENDO_TABLE`, and the **crowd runs before the line's percentage gate**, so a refused announcer line still cheers. `CROWD_DUMMY`'s own `crowd_dummy_exists` flag — the reason a row carries a duration at all — is modelled as the match's countdown, so a second draw inside one sound's life cheers without re-triggering the sound.

- **A pin now ends the round: `ANI_CODE` 2,147 -> 2,186 of 2,303 (93.2% -> 94.9%), 106 -> 109 names.** The third of the six blockers. Until now a round could only end by knockout — `wm_arcade_round.h`'s `get_live_bits` plus the 5-second pin-idiot-check countdown — and a wrestler who actually got pinned just lay there while that clock ran.
- **`win_announce` (39 uses) is three instructions and one process.** `DNKSEQ2.ASM:5202` CREATEs an `ANNC_PID` running `LIFEBAR.ASM:2642 announce_rnd_winner` and calls `KILL_PIN_HIM`. The process is where a round ends, and `wm/arcade/wm_arcade_round_announce.h` translates it down to the round award and the end-of-match announcement, with the two buckoff helpers it leans on (`anyone_bucking`, `set_all_buckoffs`) and `set_winner`'s own KO-branch winner search — "the first live wrestler, preferring one whose `DID_PIN` is set", plus the `#bogus` fallback the source's own comment calls *"really bad and should never happen"*, which is translated because a pin genuinely can leave the pinned man's whole side dead. The buckoff branch is the interesting half: if anyone is trying to buck off, the process sleeps 90 ticks at `arw_bwait`, wakes, clears everyone's `NEW_BUCKOFF`, and **only then** asks `get_live_bits` whether both teams are somehow still alive — the straight-through pin path has no such check, which is exactly why a pin ends a round outright.
- **`grnd_hit` (28 uses), "FOR USE ONLY IN PINS!"** — and the source argues with itself in the margin about whose pointer to use, one comment saying use `CLOSEST` because you do not attack during a pin and the next overruling it: *"Un! DO use WHOIHIT, as this is set in all the pin_anims before this function is called."* It puts the victim into **his own** `hitonground` animation (indexed by the victim's number, through the same `change_anim1a` seam `ANI_SLAVEANIM` already uses) and aligns the two in Z, pinner one unit in front — unless the pinner is the Undertaker, who is left where he is. Its `#hit_t` is a per-wrestler table no opcode names, so `wlpuppet.py` gained a fourth kind keyed by the routine that reads it: the *site* is named by hand, the nine rows are still read out of `DNKSEQ2.ASM`, and a test checks each one is a program this port can actually play.
- **`#setopp_deadanim`** sets `M_DEAD_ANIM` — PLYR.EQU's *"If killed, don't change anim"* — on whoever this wrestler is attached to, so a victim who dies mid-move finishes the move instead of being yanked into his death animation.
- **The ROM Makefile had drifted again, and the link check caught it.** `tools/n64_link_check.py` named all three new symbols as defined-in-`match.c`-but-not-in-the-ROM-source-list before the first commit of them. That test exists because the N64 target is built with libdragon, which is not in this checkout, so nothing else ever compiles it.

- **Four more, and then a shipped defect underneath all of it.** `shake_all_ropes` (2 uses) wobbles all four banks at once; `get_leap` (3) decides whether a wrestler may lunge, from `mv_tbl` — four live rows in sixteen, because `NEW_FACING_DIR` is only ever 5, 6, 9 or 10. `hit_nearest` (8 uses) sets `WHOIHIT` one way, the victim's `WHOPINNEDME` the other, and the victim's `PINNED` bit; `set_tbukl_confine` (11) is the turnbuckle climb asking whether the ring is allowed to hold you in — an opponent outside the ropes grants `MODE_NOCONFINE`, and otherwise you are yanked back in through `calc_line_x`. Two details worth keeping: Yokozuna skips the sweep outright and is always confined, and the "everyone on the enemy side is dead" flag does not make the routine give up — it makes it *stop skipping the dead*, so a dead man outside the ropes still grants `NOCONFINE`.
- **A local label is not scoped to its file. It is scoped to a BLOCK, and the source reuses the same name inside one file with a different body.** This is the file-scoping trap one level deeper, and it had shipped. `BAMSEQ2.ASM` defines `#set_zvel2` twice — `-5.0` at 4287 and **`+5.0`** at 4371, *opposite directions*. Every one of the eight sequence-2 files does the same. `UNDSEQ3.ASM` defines `#inc_loop` at 168 capped at 3 and again at 2888 capped at **2**; `SHNSEQ3.ASM` defines `#reverse_xvel` as a quarter of the speed at 1279 and a **half** at 3651. The registry keyed rows on `(name, file)`, so for every one of those pairs half the call sites in that file were running the other routine.
- **How a call site is resolved, and how that is known.** A `#name` reference takes the next definition at or after it, falling back to the last one before. That is not a guess at the assembler: it is what the source does in every case in the drop — each helper is written immediately *after* the animation body that calls it, so `BAMSEQ2`'s uses at 4248 and 4341 sit just above its definitions at 4287 and 4371 — and the opposite rule leaves `HRTSEQ3.ASM`'s sixteen `#rope_check` calls pointing at no definition at all, since the only one is 200 lines below the last of them. The emitter now carries the resolved definition line into the op, `wm_anim_code_run` takes it, and registry rows carry it. **All 321 local call sites resolve to a definition; none falls back.**
- **The guard, and what it is really checking.** A source-tools test asserts three things: every numbered row's `(file, line)` really is that label in the source, every file that defines a translated local name more than once has a row for *every* definition, and the emitter left no local call site unresolved. Confirmed to fail against exactly the shipped shape — one unnumbered `#set_zvel2` row for `BAMSEQ2.ASM` — before the fix.
- **And the coverage number was wrong, in my own favour.** It counted a call site as covered when its *name* was translated, so a routine translated for one file counted as translated for every file that called it, and a definition with no row counted as covered because a sibling definition had one. `tools/ani_code_census.py` now resolves each call site the way the runtime does — this definition, else this file's row, else a global. The honest figure is **`ANI_CODE` 2,206 of 2,303 (95.8%), 198 of 236 routines** — 236, not 145, because 145 was a count of *names* and the same name is repeatedly several different routines. All four systems the earlier entry called blockers are now done.

- **The fourth blocker, the renderer: `ANI_CODE` 2,206 -> 2,288 of 2,303 (95.8% -> 99.3%), 198 -> 226 of 236 routines.** Every routine in this group is the same three lines — decline when debris is switched off, draw a count, and `CREATE` that many `DEBRIS_PID` processes running a named effect — and only the last of those needs an object system. So the decision is translated and the pixels are a seam: *what* effect, on *whom*, *how many*, and the Y offset up his body. `create_impact` (14 uses) reads that offset from `SPECIAL.ASM:347`'s `#offset_t` by the **victim's** wrestler number, because the explosion has to land on a head and the heads are at different heights — Shawn's at 80, Razor's and the Undertaker's at 108.
- **`no_debris` and `reduce_bog` are state, not part of the seam, because the routines write them.** `create_impact4` and `create_impact_flykick` set `no_debris` themselves, with the source's own reason — *"Don't allow other debris to come out and bog us down!"* — and `LEXSEQ2.ASM`'s `#stop_debris` / `#restore_debris` save and **restore** it around a move, which is why restoring to a hard "on" would be wrong. `reduce_bog` is `init_reduce_bog`'s "active wrestlers minus two", so a 1-on-1 match runs with debris on.
- **Three of them have no gate at all, and that is in the source, not an omission here.** `create_impact_salt`'s and `DO_EYES`' `no_debris` checks are *commented out*, so the salt and the eyes fly whatever else is going on; `create_bucket_salt`'s own `no_debris` set is commented out too. `create_impact_salt` is also the one aimed at the wrestler **himself** rather than his victim. `DO_EYES` and `BROKEN_ARM_BLOOD` draw their counts (`movk 3 / RNDRNG0 / INC / INC` — two to five eyes; two to four blood globs on top of two fixed splashes), and the tests pin the real range rather than a single draw.
- **Plus three that only ever needed reading.** `#get_off` / `#get_off4` (3 uses) are climbing off a pinned man, one facing each way — the 4-facing one shoves backwards in Z and hops half as high. The guard earned its keep here immediately: `DNKSEQ2.ASM` turns out to define `#get_off` **twice**, at 3.0 of Z and at 4.0, and the test refused the single row before it was ever committed. `#close` (1) asks whether the two have drifted apart, and the sense is worth pinning: `MODE_STATUS` is cleared first and set for FAR, so it means *not* close, and the test is `jrgt`, so exactly 30h apart still counts as together.
- **`start_sparks` reads its own label placement, and it is easy to get backwards.** The `#rets` its debris gate jumps to sits ABOVE the second `CREATE`, so the sparks are gated and `MAYBE_SHOCKING` is **not**: the announcer gets his line even with debris switched off. `MAYBE_SHOCKING` itself is `triple_sound 3eh`, then `SLEEP 92`, then a one-in-five `SHOCKING` — the sleep is a counter here, beside `razor_swear_exists`' own lockout.
- **The two `#fireball` are the local-label find paying for itself.** `UNDSEQ4.ASM` defines the name twice, and they are different spirits — `und_spirit_pull` for the old ones, `und_spirit_push` for the reaper. Neither could have been translated at all under a `(name, file)` key.
- **Plus the palette pair and the screen flash.** `#flsh_wht` (4 uses) is `WRESTLE2.ASM:3575 flash_white`, a `QDMAN` fill of the whole screen — colour `1111h` over `[256,400]` at the origin, all real numbers. `#set_pal` / `#restore_pal` are Bam Bam turning blue as he burns; with no palette system the `pal_getf(BAMBLU_P)` resolve returns nothing and `OBJ_PAL` is left alone, but the `MY_PAL` save and the `TEMP_PAL` bit still happen, so `#restore_pal` puts back exactly what was there either way.

- **`ANI_CODE` is finished: 2,303 of 2,303 (100.0%), all 236 routines.** The last group all ended in one place — `TABLES.ASM:43 set_target_offsets`, and the grid behind it answers "where on this wrestler is his head right now", which is not one number: it depends on which wrestler and what he is doing. 26 `PLYRMODE`s collapse onto **four** blocks of 9 wrestlers × 5 body areas × X/Y/Z, and `tools/wltarget.py` reads all of it. `get_mode_height` reads the *same* grid a different way — `15n + 1` words in, which is wrestler n's head Y — so the two agree only if the row stride really is five areas of three, and the test checks that agreement rather than the extractor's own arithmetic.
- **That also made `ANI_TARGET` a real op — 36 more emitted, and the thing it does is not what its name suggests.** `ANI_TARGET,TGT_HEAD,TGT_KNEES,ATM_CLOSEST` picks whichever is *nearer*, and nearer is decided by the two wrestlers' **flip bits**, not by distance: matching flips means you are looking at his feet, so the higher area index is the near end. The source's own caveat comes with it — *"This assumes that victim is on the ground. If he's not, the results will be screwy."*
- **Two `#set_target`s in one file, doing different things, and a comment that lies.** `DNKSEQ2.ASM:3092` picks head or knees by both flips past 40h of X gap and drops onto the chest inside it; `DNKSEQ2.ASM:3394` reads the **stick**. Its `#chest` label sets `TGT_KNEES` and its `#lft` comment says "go to chest" — read the labels, not the comments; the value is what ships. `elbow_tgt2`'s `#head` sets `TGT_CHEST` for the same reason.
- **And `ANI_DRAW_NAME` came free — 113 more emitted ops.** `draw_ddt_name` is `MOVE_NAME_ANNC` with index 16 (the DDT, whatever its own "Draw hip toss message" comment says), and `ANI_DRAW_NAME` is the identical call with its operand. Drawing needs a renderer; *deciding* does not, and there are three real gates: the `reduce_bog` skip, "is one already up on the other side", and **"Messages display the first time only"** — one bit per index across two 32-bit words. Index 41 is exempt from both the bog skip and the shown-once bit, so the second wind shows every time.
- **The last four.** `#pause_opp` freezes the man you hit on whatever frame he is on by writing 25 into his animation's own tick counter from outside — like `ANIPC`, a field on the wrestler that the interpreter picks up. `#set_opp_xy` is the hip toss letting go, with a gate worth keeping: thrown from *outside* the ring, the X shove only happens within `0A0h` of the ring's middle. `#attach_victim` is `hit_puppet_even_if_dead` ("pretend collision"). `#set_new_position` is Bam Bam's reappear table, and the source's own bound is `+350` onto `+25` — 375 from the left edge, not the `400-25` its comment claims.
- **The emitter moved too: 51,168 -> 51,322 ops, 2,102 -> 1,953 skipped (96.1% -> 96.3%).** `ANI_TARGET` and `ANI_DRAW_NAME` were both in the skipped column and are not any more.
- **There is a test that it stays finished.** Not a percentage in a report: a check that every one of the 2,303 call sites resolves the way `wm_anim_code_run` resolves it, which names the routine when one stops. Confirmed to fail against a removed row.


- **The button-mash mechanic is real now.** `ANI_IF_BUTCOUNT_GE` (ANIM.ASM:3214) and `ANI_IF_BUTCOUNT_LT` (:3239) are translated -- **239 uses across the eight playable wrestlers' sequence files**, and the mechanic behind repeated knees, the combo strings and holding a grapple. Each reads one of `PLYR.EQU:152-156`'s five adjacent per-button press counters and branches against a threshold; `WRESTLE.ASM:4681 count_button_presses` is translated alongside it and runs for every wrestler every tick, where the source calls it (after `update_joystat`, before `animate_wrestler`). `hrt_knees_to_head_anim` now behaves as written: connect but stop pressing kick and the span exits after one knee; keep mashing kick and it runs its full three; keep mashing **super** kick as well and the last knee takes `ANI_IF_BUTCOUNT_GE,SKICKB_COUNT,1,#do_pile` into an `ANI_CHANGEANIM`.
- **A correction that came with it.** `ANI_CLR_BUTCOUNT` clears **all five** counters, not three. The five commented-out single-`WORD` lines in the source are the unoptimised version of the three writes below them -- two of which are 32-bit `,L` and each cover two adjacent `WORD`s (punch+block, super punch+kick). An earlier pass here read the comments instead of the widths and concluded block and kick had been dropped from the reset. That clear is what makes a count mean "since this span started" rather than "since the round started", so the mistake would have left every mash gate permanently open after one press.
- `hrt_3_pile_driver_anim` had a `WM_BRET_ANIM_PILE_DRIVER3` id with **no sequence behind it**, because nothing in the port could reach it. It is now extracted, given a program, and wired. Separately, `src/generated/bret_anim_programs.c` was being generated *by hand* rather than by `scripts/regenerate_source_data.sh` -- exactly how generated data drifts from the tool that makes it -- so `wlprogram.py` gained the `--animation FILE LABEL` form (a program set spans HRTSEQ2/3/4) and the script now emits all 41.
- **Hits actually connect.** The original `ANI3` hurt-box header (`SYS.EQU`'s `IANI3X`/`Y`/`Z`/`ID`) is confirmed unrecoverable from the checked-in tree: its `bretimg.tbl`/`bret.seq`/`imgtbl.glo` compile output is absent, and the WIMP `.IMG` directory format itself never carried it either (only `xani`/`yani`/`width`/`height`). `wm_bret_hurt_box_for_frame` (`wm/bret_backend.h`) substitutes each frame's own real WIMP image bounds instead -- real numbers, from a new geometry-only generated table (`tools/bret_geometry_bundle.py` -> `src/generated/bret_frame_geometry.c`) deliberately kept separate from `wm_bret_sprite_find`'s pixel/palette data, so hit-box math is engine logic that doesn't depend on which artwork happens to be linked in (host `ctest` included) and keeps working once stock art is replaced. `wm_bret_backend_tick` sets a real `hurt_box` from it every tick, and `wm_match_tick` now calls the real, ctest-verified-since-fix38 `wm_arcade_check_wrestler_collisions()`/`wm_arcade_wrestler_hit()` (`REACT1.ASM`) every tick via `wm_arcade_wrestler_hit_collision_callback`, with a real `adjust_health` translated from `LIFEBAR.ASM:1547-1670` (life clamped to `[0, LIFE_MAX]`, the 20+pt near-death "fudge", and "attract mode never dies"). A wired attack whose real attack box overlaps a real hurt box now genuinely reduces the victim's life.
- **A wrestler can actually die.** `wm/arcade/wm_arcade_lifebar.h`'s `wm_arcade_adjust_health` is the one real implementation of `LIFEBAR.ASM::adjust_health`'s damage-application tail, shared by both of its real callers: the `REACT1.ASM` hit path above, and `BRET.ASM mode_normal`'s own `I_WILL_DIE` self-death case (`wm_bret_backend_callbacks` now wires `adjust_health` for real instead of leaving it `NULL`, so that path no longer silently no-ops). Once life genuinely reaches 0 (not saved by the fudge or "attract mode never dies" rules), it sets a real `WM_PMODE_DEAD` and turns off further hit checks (`SETMODE DEAD` + `wres_collis_off`) -- or, when the attacker has an active combo, defers death by LIFEBAR's own 1-HP combo-revival rule (`I_WILL_DIE`, real but currently unreachable since nothing in this port sets `combo_count` yet).
- **Ordinary play actually throws a real punch.** `wm/arcade/wm_arcade_closest.h` translates `WRESTLE.ASM::calc_closest`'s per-opponent delta/distance computation (`closest_xdist`/`closest_ydist`/`closest_zdist`/`closest_dist` against `actor->smart_target`), and `wm_match_tick` now calls it every tick before `wm_arcade_move_bret`. `BRET.ASM`'s `do_punch` (`near(a,50,45)`) used to see a permanent `closest_xdist`/`closest_zdist` of 0 -- nothing computed them -- so every punch resolved to the unwired `WM_BRET_ANIM_BUTT2/4` headbutt regardless of range. With real distances, a human punching from realistic range now gets a real, wired punch animation and its real `ATTACK_ON` window through ordinary human-input play, not just direct backend calls.
- **A knockout is now genuinely detected.** `wm/arcade/wm_arcade_round.h` translates `WRESTLE2.ASM::match_timer`'s actual round-ending trigger on a KO -- not just its round clock running out: `get_live_bits()` (which `PLYR_SIDE`s still have a live wrestler) plus its real 5-second "pin idiot check" countdown once one side doesn't. `wm_match_tick` steps this every tick; once one side has been fully dead for 5 real seconds, `wm_match_state.round_state.decided`/`decided_winner_side` become real and correct (a simultaneous double-KO resolves as a draw rather than guessing at the unported PIN system's tie-break).
- **A round win is now genuinely scored, best-of-3 and all.** The instant `round_state.decided` flips, `wm_match_tick` awards it through `wm_arcade_match_score_award_round`, translating `LIFEBAR.ASM::set_winner`'s real KO branch: `wm_match_state.score` tracks real `p1rounds`/`p2rounds` and sets `match_winner` (1 or 2) the moment either side reaches `LIFEBAR.ASM`'s actual best-of-3 threshold of 2. The source's own `DID_PIN`-priority search among several survivors is moot here -- this port's fixed 1-on-1 match never has more than one wrestler per side to find. Nothing acts on `match_winner` yet -- see the boundary below.
- **Rapid hits actually deal less damage.** `wm_arcade_adjust_health` now stamps `victim->last_damage` with the real tick count on every call (`LIFEBAR.ASM:1593-1595`), unconditionally regardless of which of its own branches ran. `wm_arcade_wrestler_hit` (`REACT1.ASM`) already read `last_damage` for its rapid-hit reduced-damage window (a second hit within 50 ticks deals `WM_RD_*` instead of `WM_D_*`), but nothing had ever written it, so repeated attacks always dealt full damage regardless of timing -- a real gameplay difference, not just internal bookkeeping.
- **The first-hit and high-risk damage bonuses actually apply now.** `wm_arcade_adjust_health` also translates `LIFEBAR.ASM:1429-1466`'s own damage-shaping step: a nonzero `DAM_MULT` scales the incoming delta by `(1+DAM_MULT)/2` and is cleared, and an attacker with an active `COMBO_COUNT` replaces the delta entirely with `-max(10-COMBO_COUNT, 4)` (the second case is currently unreachable, same as the combo-revival case above -- nothing sets `combo_count` yet). `wm_arcade_wrestler_hit` was already real and ctest-verified since fix38 about *setting* `runtime->dam_mult` to 2 (first hit of the round) or 4 (a high-risk bonus hit), but nothing had ever consumed it, so those bonuses were computed and then silently discarded -- they now genuinely deal more damage.
- **Every hit is scaled the way a real 1-on-1 non-team match actually scales it.** `wm_arcade_adjust_health` now also applies `LIFEBAR.ASM:1471-1521`'s `damage_mod_table` lookup to any damage (never healing): the real table scales by active-drone-count and whether the target is a drone or a player, but `wm_match`'s `actors[]` is always the fixed pair `wm_match_start_attract`/`selected` create, so that count is always 0 -- and both of the table's 0-drones columns are the identical `_85PCT` (218/256) anyway, so no `PLYR_TYPE` branch was even needed.
- **`speed_adjustment` uses the arcade's real factory default, not a guess.** `LIFEBAR.ASM:1524-1528`'s further game-speed scaling reads an operator DIP-switch/menu setting (`ADJSPEED` via `GET_ADJ`) this port has no live settings subsystem for -- but rather than inventing a value, `WM_ARCADE_SPEED_ADJUSTMENT_16_16` uses the arcade's own factory-shipped default: `AUDIT.ASM`'s `FACTORY_TABLE` lists `ADJSPEED` (adjustment id 25) as `3`, exactly matching `LIFEBAR.ASM`'s own `BADCHK` fallback for an out-of-range read, and `LIFEBAR.ASM`'s `#timer_table` entry for that value is literally commented `"normal damage (default)"` -- i.e. the real factory default is precisely a 1.0x identity multiply. `wm_arcade_adjust_health` applies the real multiply-and-shift unconditionally (healing and damage alike); it's a no-op today, but wired for a live value the moment this port gets an operator-settings system.
- **Death doesn't just leave a wrestler as a statue anymore.** `wm/arcade/wm_arcade_mode_dead.h`'s `wm_arcade_mode_dead` translates `DOINK.ASM:2920`'s `mode_dead` -- the shared MODE_DEAD handler every wrestler's own `#mode_table[9]` points at, `BRET.ASM` included -- and `wm_bret_backend_callbacks` now wires it into `wm_arcade_move_bret`'s own `MODE_DEAD` dispatch instead of leaving it `NULL`. The real routine's job is deciding whether a freshly-dead wrestler gets a button-mashing "buckoff" comeback chance, gated on `@royal_rumble`, `is_8_on_1()`, and a combo-meter check (`CHECK_COMBO_GO`); all three are provably always false/zero in this port (no royal rumble mode, no 8-on-1 ladder battles -- both need the unported ladder/team system -- and no per-player combo-meter-fill tracking at all, since only life itself is real via `init_life_data`), so every branch collapses to the same real outcome: `WM_STATUS_NO_BUCKOFF` gets set once on a wrestler's first dead tick, and every tick after that is a genuine no-op through the source's own early-out checks. The buckoff/zombie-revival system those gates protect stays real (in the source) but unreachable here for the same reason.
- **A death actually knocks you down now, not just off.** `wm_arcade_adjust_health` (`wm/arcade/wm_arcade_lifebar.h`) also translates `LIFEBAR.ASM`'s own death-dispatch tail: `ROLL_POS` resets, and the victim's own `player_mode` at the moment of death -- read before it's overwritten to `WM_PMODE_DEAD` -- selects the real death animation exactly like the source's own `#fall` check. Every mode Bret's own real dispatcher can actually be in when the killing hit lands (`NORMAL`/`RUNNING`/`INAIR`/`INAIR2`/`BOUNCING`/`ONTURNBKL`/`BLOCK`/`DIZZY`/`CLIMBTURNBKL`) resolves to the source's own `#fallbk`: the real `hrt_fall_back_anim` (already wired as `WM_BRET_ANIM_FALL_BACK` by Bret's own `I_WILL_DIE` self-death case) fires, plus a real knockback -- unless already moving away faster than 2.0 px/tick, the corpse's `x_vel` is forced to +/-2.0 away from whoever landed the killing hit, matching the source's own default-then-override construction exactly (including its real quirk of only checking rightward speed). This is wired through both real death paths -- the `REACT1.ASM` hit path and `BRET.ASM`'s own self-death case -- via a small death-anim callback bridge, so it fires whether the human's own punch kills the opponent or Bret kills himself mid-combo. The source's own `ATTACK_MODE`/`DEAD_ANIM`-bit pre-checks that skip the whole dispatch (certain finishers, or a victim already carrying `WM_STATUS_DEAD_ANIM`) are translated too, for real, even though none of Bret's 7 wired attacks currently trigger them.
- **Secret moves genuinely respond to real joystick input now.** `wm/arcade/wm_arcade_joystat.h` translates `WRESTLE.ASM`'s shared `update_joystat` (a real 16-entry-deep per-wrestler input-history ring buffer: a facing-relative direction, remapped through the source's own `xflip_table` when facing left so "toward"/"away" always track the way Bret's actually facing, OR'd with the absolute left/right bits and, on a button press, that one button's own bit) and `check_secret_moves` (a backward-walking value/mask matcher: the newest entry must be the exact trigger, later steps get a shared budget of up to 8 skipped "noise" entries, and the whole gesture must land inside its own real timing window) -- wired into `wm_bret_backend_callbacks.check_secret_moves`, which `wm_arcade_move_bret` already called every tick but nothing ever answered. `wm_arcade_bret_secret_patterns`' 8 real value/mask sequences and `wm_arcade_bret_fire_secret`'s own per-move condition/dispatch logic (neck grab, grab fling, hip toss, face rake, jump kick, super cut, and their variants) already existed from an earlier pass; a human genuinely typing one of these sequences (e.g. down, down, punch for the super cut) now reaches them for real, not just through a direct call. And it sticks: fixed a real, pre-existing bug where `WM_BRET_SECRET_SUPERCUT` dispatched to a separate, permanently-unmapped `WM_BRET_ANIM_SUPER_PUNCH4` id, when `BRET.ASM`'s own `#scrt_cut` ("movi hrt_4_super_punch_anim,a0") selects the exact same real animation `WM_BRET_ANIM_SUPER_PUNCH2_4` already maps to -- reusing that id gives the super cut real frame data, its existing attack window, and real `WM_MODE_UNINT`/`ANI_SETFACING` protection for free (the now-dead `SUPER_PUNCH4` id is gone). The other 9 secret-move target animations genuinely have no extracted frame data yet, but their own real HRTSEQ headers do all lead with the same `ANI_SETMODE,MODE_UNINT|MODE_NOAUTOFLIP` as the 6 already-wired attacks (verified by reading each one directly, not assumed) -- `wm_bret_backend_change_anim` now applies that protection for them too, cleared back off the same tick by `wm_bret_backend_tick` rather than left set indefinitely, since there's no real frame timing to know when they'd naturally end. A deliberate, bounded simplification (one tick of real protection, not the source's full per-animation duration), not a guess at timing this port doesn't have. Three more real, related mechanisms are wired now too: `wm_arcade_update_joy_dtime` (`wm/arcade/wm_arcade_combat.h`) translates `WRESTLE.ASM`'s own `update_joy_dtime` -- a real per-button hold-duration counter, reset to 0 the instant that button is released. `bret_secret_moves`' own first entry, `#charge_ddt` (executable hold-test code, not a value/mask table), now uses that real dtime and is checked every tick ahead of the pattern scan, exactly like the source's own early return. And `hrt_charge_flying_kick`/`hrt_charge_face_rake` (BRET.ASM:543/614) -- two more independent "hold the button, then release it" watchers with real condition/dispatch logic (`wm_arcade_bret_release_charge_flying_kick`/`release_charge_face_rake`) that just never had a caller -- fire for real too. Both defer their animation through the real `SPECIAL_MOVE_ADDR` handoff (`WRESTLE.ASM`'s own `move_wrestler`: "if a special move watchdog proc has queued up an anim, do that instead of calling move_xxx") rather than setting it directly like `#charge_ddt` does, since they're genuinely separate processes in the source -- `wm_arcade_move_bret` now consumes that handoff for real, which is also the one piece that would let `wm_arcade_bret_fire_monitor`'s own real dispatch logic (headhold moves, roll uppercut, grab-toss-air, the finishers) ever take visible effect, though none of those are reachable yet without their own real trigger (a persistent input-monitor scheduler this port doesn't have, or, for the headhold moves, a grapple state this port's Bret dispatcher never enters).
- **A CPU opponent genuinely fights now.** `DRONE.ASM`'s AI decision core and its ~9-opcode script bytecode interpreter (`wm_arcade_drone_main`/`wm_arcade_drone_script_step`, `wm/arcade/wm_arcade_drone.h`) were already fully built from an earlier pass but never given real data or a way to run any of DRONE.ASM's own "DS_CODE" inline-native-code blocks (drone_chkrun's ring-safety check, drone_chrg's charge-move trigger, drn_run/drn_roll/drn_seek/drn_retreat/drn_climbtb-drn_ontb/drn_inair/drn_opinair/drn_oppdead's own movement/positioning logic, drone_seekdirdist's real sine-table circling math) -- so a drone-controlled actor never actually pressed a button. `wm/arcade/wm_arcade_bret_drone.h` now supplies both: every one of those DS_CODE blocks translated as real callback bodies (extending `wm_arcade_drone_callbacks_t.script_call`/`script_seek` to report back the skip/abort/redirect outcomes DRONE.ASM's own `addk 32,a9` / `clr a9` / `jump a1` tricks need, and fixing a real, previously-dormant bug in `WM_DRONE_SC_DONE`'s handling of DS_SLP1 -- it was clearing the script instead of just yielding one tick, unexercised until this pass's own scripts became the first real data to use it), the `blkbase_t`/`blkatk_t`/`sklhhdly_t`/`sklhrdly_t`/`sklrep_t` skill tables (DRONE.ASM's own `SKLM` macro, which turned out to be very much present in the checked-in tree -- an earlier, incorrect claim that it wasn't is corrected here), and Bret's own real short/medium/long-range script lists (`bret_s_t`/`bret_m_t`/`bret_l_t` and everything they transitively reference: `brM_n`/`brMm_n`/`brM_hh`/`brM_hhr`, the generic `M_og`/`Mm_og`/`M_opptbkl`/`M_shrtblkr[dl]` lists, and every primitive action script). Since only Bret has a real move/animation backend in this port, `range_script_list`/`drn_combo`/`drone_chrg`'s per-wrestler branches only ever hand out real attack content when the drone-controlled actor itself is Bret (`wrestler_num==WM_ROSTER_BRET`) -- for any other wrestler they return NULL/no-op, same boundary as the bullet above -- but the shared, wrestler-agnostic majority of the engine (positioning/seeking, block detection and its real skill-scaled percentages, getup timing, run/roll/turnbuckle/in-air/dead-opponent handling) now runs for any wrestler pairing. `wm_match_tick` also now computes `wm_arcade_calc_closest` for every actor every tick (previously only for whichever one carried a real Bret backend, so a drone actor's own block/range decisions read a tick-stale distance). One real, already-established boundary carries over exactly: `CHECK_COMBO_GO` (`wm_arcade_mode_dead.h`'s own prior finding) always reports "not lit" in this port, so `drn_combo`'s button-mash combo-punch finisher -- fully transcribed here for completeness -- is real but, like the source's own empty combo meter, never actually reachable. A CPU-controlled Bret now genuinely walks toward, blocks, and attacks a human or another CPU Bret with real button/stick presses driving the real `wm_arcade_move_bret` dispatch, not an idle stand-in. Reading DRONE.ASM's own opponent-mode dispatch closely (WRESTLE.ASM's `wres_get_stick_rel_cur`) also turned up a real, pre-existing bug in `wm_arcade_drone_main`'s own reversal-direction check, from the engine's earlier pass: choosing between the `M_shrtblkr`/`M_shrtblkrdl` block-reversal scripts compared the opponent's raw `stick_val_cur` against `WM_MOVE_DOWN_LEFT`, when the source reads `STICK_REL_CUR` (`PLYR.EQU`: "facing reletive") -- the same facing-relative value already computed elsewhere in this port (`flip_lr_dir`) -- so the choice was silently swapped whenever the opponent was facing left. Fixed to compute the real facing-relative value first.
- **All eight wrestlers actually fight now, not just Bret.** `wm_arcade_move_ported_wrestler` -- the generic 8-wrestler dispatcher, with all eight real per-wrestler direct-port modules (`wm_arcade_bret/razor/taker/yoko/shawn/bam/doink/lex.c`) behind it -- was already built and ctest-verified, but `wm_match_tick` still hardcoded a direct `wm_arcade_move_bret` call, so every other wrestler held real state and never moved. It now dispatches every actor through the real dispatcher. Three things had to become real for that to mean anything. First, `wm/wrestler_backend.h`: the shared, animation-free half of a backend for the seven wrestlers that don't have one -- real `execute_walk` (`WRESTLE.ASM`'s own `execute_walk`/`set_velocities`) driven by that wrestler's OWN velocity table, transcribed from each file's own `xxx_velocity_table` and its `#VEL`/`#DVEL` (`BRET.ASM:2848`, `RAZOR.ASM:2586`, `TAKER.ASM:3145`, `YOKO.ASM:2662`, `SHAWN.ASM:3203`, `BAM.ASM:2810`, `DNK.ASM:2794`, `LEX.ASM:2619`) -- seven share `3a000h`/`31000h` and **Doink is the one genuine outlier at `30000h`/`21f0eh`, i.e. really is slower** -- plus the same shared `adjust_health`/`mode_dead`/`check_combo_go` every other caller uses. Second, every wrestler's own real AI script tables from `DRONE.ASM` (`raz_s_t`/`utak_s_t`/`yoko_s_t`/`shawn_s_t`/`bam_s_t`/`doink_s_t`/`lex_s_t` and their `xxM_n`/`xxMm_n`/`xxM_hh`/`xxM_hhr`/`xxM_ooh`/`utM_chold` lists, plus ~20 more primitive action scripts they reference), joining Bret's from the previous pass -- each wrestler's own `BBL` chain differs, so the dispatch honors it (Bam/Yoko/Lex have an extra `MODE_OPPOVERHEAD` row, Undertaker a `MODE_CHOKEHOLD` row, Undertaker and Yoko share the generic `M_hhr`). Third, two real bugs this surfaced, both of which had been silently disabling AI behavior: `wm_match_tick` never set `wm_arcade_drone_world_t.pcnt` (`@PCNT`), leaving it pinned at 0 -- which made `drone_main`'s own "(PCNT+1) low four bits clear" passive gate never pass, so **a MODE_NORMAL drone could never reach `#doact` and never selected an action script at all**, Bret's included, while its every-32-ticks aggression reroll fired every single tick instead; and a wrestler that entered `MODE_BLOCK` could never leave, because `mode_block`'s own release check is gated behind a newly-pressed button exactly like the source, while the ungated release actually lives in the block animation (`ANI_WAITRELEASE,PLAYER_BLOCK_BIT` -> `ANI_SETPLYRMODE,MODE_NORMAL`) -- `wm_wrestler_backend_tick` now supplies that one transition for the wrestlers with no animation layer. All eight now genuinely walk, close distance, block, and throw their own real attacks against each other under CPU control; see the boundary below for what they still can't do.
- **The AI was silently not random at all, and Bret can finally block.** Two real bugs, found by asking why a CPU-vs-CPU Bret match never threw a punch. First, `@RAND` was degenerate: `WRESTLE.ASM`'s randomize step is `rl RAND,RAND / rl HCOUNT,RAND / add sp`, and `add sp` is the *only* part that can change RAND's value -- everything before it just rotates bits. This port supplied neither HCOUNT nor SP and seeded RAND from 0, so RAND rotated 0 to 0 forever and **every `RNDRNG0` in the game returned 0**. Every drone table roll therefore picked index 0, which is why the AI only ever chose `#run` (`P_M+K_M`) and never a single attack -- 107 button presses in 3000 ticks, all of them the same one. `app.c` now feeds both inputs from the real source clock (the same beam-position surrogate its own title-sparkle RNG already used), and `wm/arcade/wmania_rng.h` documents the trap so the next integrator doesn't re-arm it; a Bret-vs-Bret CPU match now trades real punches and kicks and actually takes damage. Second, `MODE_BLOCK` is never set by any wrestler's `.ASM` -- it comes from the block *animation*'s own `ANI_SETPLYRMODE,MODE_BLOCK` (`HRTSEQ4.ASM:104` `hrt_4_block_anim`; its 2-facing twin is entirely commented out in the source, which is why `do_block` only ever selects `BLOCK4`). That animation is now extracted and wired, and the backend executes its three real commands at their traced positions: `ANI_SETPLYRMODE,MODE_BLOCK` before frame 0, `ANI_WAITRELEASE,PLAYER_BLOCK_BIT` parked on frame 1 so the block lasts exactly as long as the button is held, and `ANI_SETPLYRMODE,MODE_NORMAL` after frame 2. Bret genuinely enters and leaves `WM_PMODE_BLOCK` now, which is what makes `wm_arcade_try_attack_hit`'s own `hit_blocker` check and `mode_block`'s 160-tick push counter reachable for him at all.
- **The ring rope actually stops you now.** `wm/arcade/wm_arcade_confine.h`'s `wm_arcade_confine_wrestler` translates `WRESTLE.ASM:3074` `confine_wrestler`'s in-ring branch: `MODE_NOCONFINE`/`PLYRMODE==ATTACHED` early-out, Z bounds against `RING_TOP`/`RING_BOT`, and X bounds against the real left/right rope lines. The X check reuses fix38 Ring Chunk 1's already-translated boundary geometry (`wm/arcade/wmania_ring_geometry.h`) through a new `wm_ring_calc_line_x`, which computes `WRESTLE.ASM:5814` `calc_line_x`'s real per-Z interpolation directly (the closed form of the source's own precomputed-table setup routine, including its genuine one-step inset quirk) instead of caching a table this port has no boot phase for -- and reads `actor->hurt_box.x1/x2` exactly where the source reads `OBJ_COLLX1`/`OBJ_COLLX2`, since `wm_arcade_set_hurt_box` already computes byte-identical values (the same `set_collision_boxes` translation). Sets the real `WM_MOVE_*` bits in a new `actor->can_move_dir` field, which `wm_execute_walk` now reads for the real down-right/down-left redirect (`WRESTLE.ASM:5339-5384`) instead of always taking the un-redirected diagonal. Wired into `wm_match_tick` for the Bret actor only, right after its hurt_box is set fresh each tick. The source actually runs `confine_wrestler` twice per tick and ORs the results (`confine_wrestler_fix1`/`fix2`) -- that only converges because the collision box gets refreshed from the corrected position before the second call, and neither that second call site nor what refreshes the box is located in the checked-in source, so (confirmed by testing) a second pass reusing the same stale box diverges rather than converges; only the genuine single real pass is kept.

## What is deliberately *not* called a port yet

- The rest of `start_match`: `INIT_LADDER_TABLE`/`CURRENT_LADDER`/`NUM_OPPS` multi-drone team selection (both `#0plyr` and `#1plyr` fall back to one placeholder opponent), ring/rope/crowd/timer process creation, collision/pin/finisher lifecycle, and the `#2plyr` (two-human) path.
- **Visuals for the other seven wrestlers.** This entry used to say they "set no attack box and no hurt box -- so they can neither land nor receive a hit". That is no longer true, and the animation VM is what changed it: all seven run their real ANIM.ASM programs through the generic backend, so every frame sets a real `hurt_box` from real WIMP image bounds, `ANI_ATTACK_ON`/`ATTACK_OFF` fire at their real positions, and `wm_arcade_confine_wrestler` applies to all eight. `test_every_wrestler_can_land_a_hit` now drives each of the eight through the generic backend and the real collision pass and asserts the punch connects and the victim loses health -- with no per-wrestler code behind any of them but the source's own animation. What is genuinely still missing is **pixels**: only Bret has a sprite bundle (`scripts/prepare_bret_sprites.sh` turns a frame-name list into CI8 data), so the other seven are not *drawn*. The input that pipeline needs already exists: the emitted programs name **4,739 distinct frames across all eight wrestlers**, so generalising the bundler off the VM's own frame names, rather than a hand-kept list, is the next real step. The geometry gap was small and *concentrated*, and is now closed: the 263 missing frames were all one category -- the poses `ANI_SUPERSLAVE2` puts the **victim** into. The bundler was already aimed at all eight `.LOD` files and at the puppet tables, but its program regex matched only `WM_AOP_FRAME`, the frames a wrestler shows *himself*, so a victim pose was collected only if it happened to appear in a puppet table too. All 263 were present in the artwork the whole time. **4,859 -> 5,122 frame geometries**, and the only frames still without any are the five `BURNBODY` effect frames and the two the original has no artwork for anywhere.
- The rest of Bret's own animation set. 23 of the 68 real animation ids his dispatcher can select still resolve to nothing (hair pickup, shooter, raise-arm, the run states, the grapple, puppet and finisher moves) -- counted directly against `wm_bret_anim_sequence` rather than by hand, which is how the previously-published "43" turned out to be an undercount. His `mode_normal` genuinely chooses them; they just play no frames, set no attack box, and fire no `ATTACK_ON` window. The extraction is scriptable per animation (`tools/wlanim.py` + a geometry regen, as the block animation below now shows end to end); the `ATTACK_ON`/`ATTACK_OFF` frame tracing is now scripted too (`tools/wlattack.py`), leaving the *reading* of what each command means as the manual part.
- `CLIMB_DOWN`, for a reason no amount of tracing fixes. `hrt_climb_down_anim` passes the slice audit cleanly, but its third and last frame is `H4HU4B+FR10` and that artwork does not exist: `BRET.LOD` maps only `H4HU4B01/02/03/04/07`, and the container it points at (`hrt_jms.img`) has no `FR10` inside it either. The source asks for a frame the shipped art does not contain, so the animation is left unmapped rather than wired two-thirds complete.
- Mid-animation `ANI_ADD_MOVE`/`ANI_ZEROVELS` pairs. `wm_bret_backend_change_anim` now fires the real `ANI_ZEROVELS` every wired attack's header carries before its own frame 0, but several of these animations also carry further `ANI_ZEROVELS` mid-swing that exist to cancel an `ANI_ADD_MOVE` this port doesn't translate -- wiring the cancel without the thing it cancels would model half a mechanism, so both are left alone as a pair. Mid-animation `ANI_SETPLYRMODE` itself is now wired (see the batch-3 bullet above), but `hrt_2/4_super_kick_anim`'s own `ANI_SETPLYRMODE,MODE_INAIR` at frame 1 is deliberately still not applied: unlike `kick_TB`, which is newly wired, adding it would change the behavior of an animation that has been shipped and tested for several cycles, so it wants its own verified pass rather than riding along here.
- The blocked-hit reaction animation. `REACT1.ASM`'s own `hrt_4_hitblock_anim`/`hitblock2` (which `ANI_GOTO` straight back into the block hold) have a real caller in `wm_arcade_react1_core.c`, but the live match's hit path stops at damage and never dispatches the per-reaction REACT2/3/5 handlers that would reach it, so a blocked hit currently registers as blocked without playing its reaction.
- The rest of `confine_wrestler` beyond the in-ring branch: `ATTACH_PROC` paired-actor movement and rope-wobble bounce velocity/sound on first contact, `ck_climb_out_top/bot/side` (already translated as pure logic in `wm/arcade/wmania_ring_climb.h` via an earlier fix38 chunk, but triggering a climb is a presentation concern this port doesn't wire to any animation system yet), the entire `#outring` branch (`ARENA_TOP/BOT`, the fence lines, and the mat2-edge climb-in overlap check -- genuinely unreached since `in_ring` never leaves `1`/true, no ring-out/knockback system clears it), and the `PLYRMODE==RUNNING` gate-crash tail (needs `FACETBL`/`FACE24TBL` animation-table dispatch this port hasn't translated).
- `wm_arcade_calc_closest`'s own scope: it only computes real deltas/distance against the one fixed `smart_target` opponent this port ever has. `calc_closest`'s multi-candidate ranking loop (`biased_range`, `ONGROUND`/`WHOIHIT`/`INRING`/previous-closest/combo/Z-distance biasing, the running-behind-us skip rule, and `CLOSEST_NUM` selection itself) is not translated -- moot until a real ladder/teammate system exists to choose among more than one opponent -- and `calc_closest2`'s every-4th-tick recompute throttle (a pure CPU-cost optimization) isn't either, since recomputing one distance every tick is trivial.
- What death doesn't do yet. Death sound (`triple_sound 034h`) is real but not wired -- this port's Bret backend leaves every other real sound call a no-op too (`wm_arcade_bret_callbacks_t.sound` is never wired anywhere), so adding a bridge for just this one call would be inconsistent. The `#grnd` convulse_t/hitonground dispatch and the `#will_die` `HEADHELD` death-deferral are both real but permanently unreachable, since this port's Bret dispatcher never sets `WM_PMODE_ONGROUND`, `WM_PMODE_DEAD`-at-time-of-a-second-death, or `WM_PMODE_HEADHELD`. `flash_red` is pure lifebar-rendering feedback with no rendering system here.
- What `match_winner` doesn't do yet. `wm_match_state.score` is real, correctly-scored best-of-3 state, but nothing in `wm_match` reacts to it once it's set: `wm_match_tick` keeps stepping every actor exactly as before -- no `MODE_DEAD` wrestler resumes moving on its own (`wm_arcade_move_bret`'s `WM_PMODE_DEAD` case is a safe dead end), but the match doesn't end, restart into round 2, or present a winner either. There is still no `PINHIM_ANIM_PID` pin-attempt window (so `set_winner`'s `DID_PIN`-priority search, moot for this port's fixed pair, would matter the moment a real PIN system existed), no announcer, no crowd reaction, no on-screen round-clock display (`match_time`, `ADJSPEED`) or its own KO-independent `#tmout` award path, and no `DO_ROUNDS` round-2 reset (life/position/`round_state` all stay exactly as they were when round 1 ended) -- multiple rounds/a full match were never in scope here to begin with (see the ladder/team boundary above).
- Full credit/buy-in/start/select orchestration: the selection mechanics/data core is translated, but original credit processes, text, mug objects and final screen rendering still need their shared backends.
- The complete 131-command animation interpreter, branch/pointer relocation, all wrestler visual banks and wrestler-specific process logic.
- Full background header/palette object construction, fonts/text, fades and remaining frontend routines.
- Collision/ropes/turnbuckles, grapples, damage/pins/finishers, original CPU behavior and DCS audio backend.

Anything still missing stays absent or is reported in the generated frontiers. Harness code does not substitute for the game.

## N64 controls

The N64 layer maps controller state into portable arcade actions. Current bring-up mapping is:

| N64 | Portable action |
|---|---|
| Analog stick / D-pad | movement / frontend direction |
| A | run |
| C-Left | light punch |
| C-Up | power punch |
| C-Right | light kick |
| C-Down | power kick |
| R | block |
| Start | arcade attract/start input path |
| Z | N64-only debug HUD |

The final frontend/gameplay input behavior follows source routines as they are translated; the harness mapping is not treated as authoritative arcade behavior.

## Host verification

```sh
cmake -S . -B build-host -DCMAKE_BUILD_TYPE=Release
cmake --build build-host --parallel
ctest --test-dir build-host --output-on-failure
./build-host/wm_headless
```

The fix38 combat/ring/RNG/attract/hiscore host tests (`tests/arcade_port/fix38/`) verify themselves with plain `assert()`. Since `-DCMAKE_BUILD_TYPE=Release` above defines `NDEBUG`, which normally compiles every `assert()` to a no-op, their CMake targets force `-UNDEBUG` so those checks stay real under the documented build (confirmed by deliberately breaking one and watching it fail).

## N64 build

With libdragon configured:

```sh
export N64_INST=/path/to/n64_toolchain
make -j2
```

Output:

```text
wm_arcade_r9.z64
```

GitHub Actions fetches the historical WrestleMania source directly, regenerates translated tables/frontier reports and required artwork, builds the ROM, uploads the `wm-arcade-r9-build` Actions artifact, and publishes the ROM plus reports to the `rom-build` branch.

## Source/regeneration tools

```sh
sh scripts/fetch_original.sh
sh scripts/regenerate_source_data.sh
sh scripts/inventory_original.sh
sh scripts/build_source_ir.sh
sh scripts/build_animation_ir.sh
sh scripts/prepare_bret_sprites.sh
sh scripts/prepare_frontend_assets.sh
```

The historical tree under `original/` is a build input and is not bundled into this repository/package.

## Coverage

`port/translation_manifest.json` is canonical. CI regenerates `docs/PORT_COVERAGE.md` from it. `partial-source` means only the stated original subset is translated. `harness-only` means test/bring-up code that is forbidden from normal arcade execution.
