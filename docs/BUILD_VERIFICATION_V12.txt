Fix39 V12 verification — source-backed ATTRACT renderer checkpoint
Date: 2026-08-20

Scope:
- Preserve the V11b ATTRACT.ASM scheduler/timing ownership.
- Bind source-backed N64 renderers for designer hints, general tips,
  two-page copyright and AAMA.
- Generate visible text from ATTRACT.ASM.
- Generate WIMP pixels/palettes from the historical source IMG set.
- Pack slateBMOD through the existing source BMOD pipeline.

Local package checks performed in the ChatGPT build container:
- Python bytecode compilation for apply_fix39.py and both ATTRACT generators.
- tools/fix39_attract_text.py --self-test.
- tools/fix39_attract_assets.py --self-test, including long WIMP symbol recovery.
- tests/test_patcher_structural.py.
- tests/test_v12_patcher.py, including idempotency, generated-header ordering,
  four renderer switch cases, capability mask, copyright page semantics and
  AAMA gradient contract.
- tests/run_fix39_smoke.sh with host C compiler.
- strict C11 syntax/warning check of the V12 renderer snippet with stubbed
  platform interfaces.
- bash -n termux_fix39_build.sh.
- final archive SHA256 manifest verification and unzip integrity check.

Current public target source was rechecked on 2026-08-20 for the N64 main.c
anchors used by the patcher.  In particular, fill_rect uses x0/y0/x1/y1 RDPQ
rectangle coordinates, matching the V12 AAMA gradient translation.

A complete libdragon/N64 ROM was NOT built inside this container because the
libdragon/GitHub CI toolchain is not present here.  The bundled Termux script is
the authoritative end-to-end path: it clones the fresh target repo, applies
V12, fetches the historical source, performs real text/WIMP generation
preflight, runs available host checks, pushes a revision branch, waits for the
existing GitHub Actions libdragon build, and copies the resulting .z64 to
Android Downloads.
