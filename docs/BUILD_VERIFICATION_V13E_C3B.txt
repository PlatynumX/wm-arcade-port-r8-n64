WrestleMania Arcade N64 - Fix39 V13e-c3b verification
=======================================================

Purpose
-------
C3b fixes the historical DRONE.ASM script-image parser after the real Termux
preflight reached `reachable script label ucu not found in active DRONE.ASM body`.

Corrections
-----------
- All assembler source labels used by script bodies, branches and command-2 tables
  are keyed and resolved case-insensitively.
- Runtime source-script and skill-table lookups use the same assembler-style
  case-insensitive comparison.
- Inline data labels (`LABEL .word ...`, `LABEL WLL ...`) are recognized.
- Williams W/L packing macros are decoded to exact WORD/LONG widths rather than
  skipped by the image builder.
- C3a include/equate fixes remain intact.
- C2e Sports / BE A MAN / PLATYNUMX data is unchanged.

Regression coverage
-------------------
The script generator self-test now deliberately references lowercase `ucu` while
defining uppercase inline `UCU`, and also includes packed W/L data.  The compiler
smoke independently exercises lowercase `ucu` -> `#UCU`.  Full Fix39, C1/C2/C3,
range, Sports, namespace and VM semantics tests pass locally.

Still gated for C4
------------------
- plain source rnd service
- drone_seek service
- command-5 / EXGPC source code-call targets
- final DRONE activation remains C5
