Fix39 V13e Chunk 5a
====================

This chunk begins the executable-service closure pass after c4e.
It adds a build-time audit against the fetched historical DRONE.ASM and the
C3-generated reachable script graph. The build now refuses to continue unless:

- the script interpreter's source `callr drone_seek` is present;
- the generated C4 executable seam count matches its emitted seam table; and
- every command-5 / EXGPC seam names a real source label/location.

This does not fake or activate DRONE. It converts the remaining service work
from an informal list into a source-verified, build-enforced manifest that the
next binding pass consumes.
