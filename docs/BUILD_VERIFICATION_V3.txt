Fix39 integrator v3 verification
================================

Reason for v3:
- v1/v2 used a byte-layout-sensitive regular expression to locate the provisional
  title RNG implementation in src/core/app.c.
- v3 locates title_rndrng0 by C function name and balanced braces, then replaces
  the function body regardless of whitespace/body drift.
- CMake and Makefile integration were also changed to structural insertion so
  the Fix39 sources do not depend on the old source-list tail layout.

Local verification performed 2026-08-20:
- python -m py_compile tools/apply_fix39.py: PASS
- all 54 Fix39 C sources + fix39_smoke.c, GCC -Werror: PASS
- structural patch against a fixture matching the live repo's RNG / MATCH_INIT /
  wm_app_video_frame forms: PASS
- second application (idempotence check): PASS
- CMake src/fix39 include path insertion: PASS
