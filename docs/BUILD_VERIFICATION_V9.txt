Fix39 V9 verification
=====================

Reason for V9:
- V8 host verification passed, then the actual N64/libdragon build failed in
  src/core/app.c because include/wm/ropes.h and wmania_rope_command.h declared
  overlapping global C enumerators (visible failure: WM_ROPE_COMMAND_COUNT).
- the compiler reported 5 enum redefinition errors before stopping that TU.

V9 correction:
- every public direct-port rope constant defined by the Fix39 rope headers is
  namespaced from WM_ROPE_* to WM_FIX39_ROPE_*
- original numeric GAME.EQU / ROPES.ASM values are unchanged
- WmRopeBank/WmRopeAction data flow and all rope resolver/runtime behavior are
  unchanged

Local source verification:
- complete 54-module Fix39 source bundle: GCC -Werror PASS
- complete 54-module Fix39 source bundle: Clang -Werror PASS
- frontend ANIM/ATTRACT namespace collision regression: PASS
- V8 rope namespace collision regression: PASS
  (old WM_ROPE_* names deliberately declared before Fix39 headers)
- Python patcher syntax: PASS
- Termux build script bash syntax: PASS
- smoke-test script bash syntax: PASS

Patcher regression:
- apply to V8 current-style fixture: PASS
- second apply preserves patched target hashes: PASS
- namespaced wmania_rope_command.h copied into repo fixture: PASS

Live wiring retained from V8:
- RAND/RNDRNG0 frontend ownership
- source ATTRACT.ASM top-level cycle ordering
- source HSTD recent-champion data
- two live wrestler dispatchers
- all eight dedicated wrestler modules
- Bret/Razor ani_init entry paths
- attachment callback boundary
- four live ROPES.ASM process banks
- rope command -> source-program interpreter path
- combat/reaction/special initialization
- exact keep_onscreen pre-velocity bridge (still gated on unported real inputs)

Authoritative remaining verification:
- GitHub Actions libdragon/N64 cross-build of V9
- real-hardware behavior of newly live runtime paths

V7 remains the user-confirmed clean build/boot/play baseline.
V8 exposed the rope enum namespace collision only when compiled in the full
repo/N64 translation-unit context; V9 adds a regression for that exact class.
